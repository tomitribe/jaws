{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"JAWS — Java AWS S3","text":"<p>Strongly-typed, proxy-based Java abstraction for Amazon S3.</p> <p>JAWS lets you define plain Java interfaces that map directly to your S3 bucket structure. The library creates dynamic proxies that translate method calls into efficient S3 API operations — no boilerplate, no manual key construction, no pagination code.</p>","path":["Home","JAWS — Java AWS S3"],"tags":[]},{"location":"#why-jaws","level":2,"title":"Why JAWS?","text":"<p>Working with the AWS S3 SDK directly means writing repetitive code to build requests, handle pagination, and assemble key paths. JAWS replaces all of that with a clean interface-driven model.</p> <p>Consider a Maven repository hosted in S3:</p> <pre><code>repository/\n  org/\n    apache/\n      maven/\n        maven-core/\n          3.9.6/\n            maven-core-3.9.6.jar\n            maven-core-3.9.6.pom\n            maven-core-3.9.6-sources.jar\n          4.0.0/\n            maven-core-4.0.0.jar\n            maven-core-4.0.0.pom\n        maven-model/\n          3.9.6/\n            maven-model-3.9.6.jar\n            maven-model-3.9.6.pom\n      tomee/\n        openejb-core/\n          10.0.0/\n            openejb-core-10.0.0.jar\n            openejb-core-10.0.0.pom\n    tomitribe/\n      tomitribe-util/\n        1.0/\n          tomitribe-util-1.0.jar\n          tomitribe-util-1.0.pom\n</code></pre> <p>With JAWS, you define interfaces that mirror this structure — each directory level becomes a typed interface, each file becomes an <code>S3.File</code> or  <code>S3.Dir</code>:</p> <pre><code>import org.tomitribe.jaws.S3;\n\npublic interface Repository extends S3.Dir {\n    Stream&lt;GroupId&gt; groups();\n    GroupId group(String name);\n}\n\npublic interface GroupId extends S3.Dir {\n    Stream&lt;GroupId&gt; groups();       // sub-groups: org/ → apache/ → maven/\n    Stream&lt;ArtifactId&gt; artifacts();\n    GroupId group(String name);\n    ArtifactId artifact(String name);\n}\n\npublic interface ArtifactId extends S3.Dir {\n    Stream&lt;Version&gt; versions();\n    Version version(String name);\n}\n\npublic interface Version extends S3.Dir {\n    Stream&lt;Artifact&gt; artifacts();\n}\n\npublic interface Artifact extends S3.File {\n    // inherits getValueAsString(), getSize(), getLastModified(), etc.\n}\n</code></pre> <p>Now navigate the repository like a typed filesystem:</p> <pre><code>import software.amazon.awssdk.services.s3.S3AsyncClient;\nimport org.tomitribe.jaws.S3Client;\nimport org.tomitribe.jaws.S3Bucket;\n\nS3Client s3 = new S3Client(S3AsyncClient.builder().build());\nS3Bucket bucket = s3.getBucket(\"repository\");\nRepository repo = bucket.as(Repository.class);\n\n// Browse to a specific version\nVersion v = repo.group(\"org\")\n    .group(\"apache\")\n    .group(\"maven\")\n    .artifact(\"maven-core\")\n    .version(\"3.9.6\");\n\n// List all artifacts in that version\nv.artifacts().forEach(artifact -&gt; {\n    System.out.println(artifact.file().getName()\n        + \" (\" + artifact.getSize() + \" bytes)\");\n});\n// maven-core-3.9.6.jar (3412507 bytes)\n// maven-core-3.9.6.pom (12841 bytes)\n// maven-core-3.9.6-sources.jar (1987654 bytes)\n\n// Stream every artifact under org/apache/maven\nrepo.group(\"org/apache/maven\").artifacts().forEach(artifactId -&gt; {\n    System.out.println(artifactId.file().getName());\n    artifactId.versions().forEach(version -&gt; {\n        System.out.println(\"  \" + version.file().getName());\n    });\n});\n// maven-core\n//   3.9.6\n//   4.0.0\n// maven-model\n//   3.9.6\n// openejb-core\n//   10.0.0\n</code></pre>","path":["Home","JAWS — Java AWS S3"],"tags":[]},{"location":"#key-features","level":2,"title":"Key Features","text":"<ul> <li>Typed proxies — Define interfaces, get proxies. Method names map to S3 keys.</li> <li>Directory &amp; File types — <code>S3.Dir</code> for containers, <code>S3.File</code> for objects with content.   Return types drive listing behavior automatically.</li> <li>Annotations — <code>@Name</code>, <code>@Parent</code>, <code>@Recursive</code>, <code>@Prefix</code>, <code>@Suffix</code>, <code>@Match</code>,   <code>@Filter</code>, <code>@Delimiter</code> give fine-grained control over key resolution, traversal,   and filtering.</li> <li>Efficient S3 usage — Delimiter-based listings, server-side prefix filtering, and   controlled walk depth minimize AWS API requests.</li> <li>Upload &amp; Download — Built-in support via the S3 Transfer Manager with progress tracking.</li> </ul>","path":["Home","JAWS — Java AWS S3"],"tags":[]},{"location":"#quick-start","level":2,"title":"Quick Start","text":"<p>Add the dependency to your <code>pom.xml</code>:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;org.tomitribe&lt;/groupId&gt;\n    &lt;artifactId&gt;jaws-s3&lt;/artifactId&gt;\n    &lt;version&gt;2.1.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>Then head to Getting Started for a complete walkthrough.</p>","path":["Home","JAWS — Java AWS S3"],"tags":[]},{"location":"changelog/","level":1,"title":"Changelog","text":"","path":["Changelog"],"tags":[]},{"location":"changelog/#210","level":2,"title":"2.1.0","text":"<p>Breaking changes</p> <ul> <li> <p>All listings are immediate by default. <code>Stream&lt;T&gt;</code>, <code>List&lt;T&gt;</code>, <code>Set&lt;T&gt;</code>, <code>Collection&lt;T&gt;</code>, and <code>T[]</code> methods now return only immediate children (one level) regardless of element type. Previously, plain <code>T</code> (not <code>S3.Dir</code> or <code>S3.File</code>) silently used a recursive flat listing.</p> </li> <li> <p><code>@Walk</code> replaced by <code>@Recursive</code>. The <code>@Walk</code> annotation and its <code>maxDepth</code>/<code>minDepth</code> attributes have been removed. Use <code>@Recursive</code> to opt into recursive listing. There is no depth control; for depth-limited traversal, use <code>S3File.walk()</code> directly.</p> </li> </ul> <p>New features</p> <ul> <li>Input validation on single-arg methods. <code>@Prefix</code>, <code>@Suffix</code>, <code>@Match</code>, and <code>@Filter</code> annotations on the return type or method now validate the input name when calling single-arg proxy methods like <code>UserFile user(String name)</code>. An <code>IllegalArgumentException</code> is thrown if the name doesn't pass. This catches invalid keys at the call site rather than silently creating a proxy for a non-matching key.</li> </ul> <p>Migration</p> Before (2.0.x) After (2.1.0) <code>@Walk Stream&lt;S3File&gt; all()</code> <code>@Recursive Stream&lt;S3File&gt; all()</code> <code>@Walk Stream&lt;S3.Dir&gt; dirs()</code> <code>@Recursive Stream&lt;S3.Dir&gt; dirs()</code> <code>@Walk(maxDepth=1) Stream&lt;X&gt; items()</code> <code>Stream&lt;X&gt; items()</code> (immediate is now the default) <code>@Walk(minDepth=n, maxDepth=n) Stream&lt;X&gt; exact()</code> Remove — use <code>S3File.walk()</code> manually <code>Stream&lt;T&gt; items()</code> (plain T, was recursive) Add <code>@Recursive</code> to preserve old behavior, or leave as-is for immediate","path":["Changelog"],"tags":[]},{"location":"changelog/#20","level":2,"title":"2.0","text":"<p>Breaking changes</p> <ul> <li> <p>Upgraded to AWS SDK v2. The library now uses the AWS SDK v2 (<code>S3AsyncClient</code>) exclusively. The v1 <code>S3Client</code> dependency has been removed.</p> </li> <li> <p><code>S3.of()</code> removed. Use <code>s3Bucket.as(MyInterface.class)</code> and <code>s3File.as(MyInterface.class)</code> instead.</p> </li> <li> <p><code>S3.get()</code> renamed to <code>S3.file()</code>. All call sites referencing <code>S3.get()</code> must be updated.</p> </li> </ul> <p>New features</p> <ul> <li> <p><code>S3.Dir</code> and <code>S3.File</code> parent interfaces. Typed proxies can now use <code>S3.Dir</code> and <code>S3.File</code> as return types to distinguish directories from files in listings. <code>Stream&lt;S3.Dir&gt;</code> returns only directories; <code>Stream&lt;S3.File&gt;</code> returns only files.</p> </li> <li> <p><code>@Prefix</code> support in <code>@Walk</code> and list operations. The <code>@Prefix</code> annotation now works with <code>@Walk</code> and standard list methods, allowing prefix-scoped recursive and flat listings.</p> </li> <li> <p><code>@Delimiter</code> works with <code>@Walk</code>. The <code>@Delimiter</code> annotation can now be combined with <code>@Walk</code> for more precise listing control.</p> </li> <li> <p><code>@Filter</code> on interfaces. The <code>@Filter</code> annotation can now be placed on the interface type itself, applying the filter to all listing methods in that interface.</p> </li> <li> <p><code>@Suffix</code> and <code>@Match</code> annotations. New annotations for filtering S3 listings by suffix or regex pattern. Both support an <code>exclude</code> flag and are repeatable.</p> </li> <li> <p><code>@Match</code> and <code>@Suffix</code> <code>exclude</code> flag. Both annotations accept <code>exclude = true</code> to invert the match, filtering out entries that match rather than keeping them.</p> </li> <li> <p>Expanded <code>upload()</code> methods. Additional overloads for uploading content from <code>String</code>, <code>byte[]</code>, <code>File</code>, <code>Path</code>, and <code>InputStream</code>.</p> </li> <li> <p><code>ObjectMetadata</code> improvements. Added <code>ObjectMetadata.from(File)</code> and <code>ObjectMetadata.from(Path)</code> for automatic content-type detection, plus a builder API.</p> </li> <li> <p>Fluent <code>put()</code> methods on <code>S3Bucket</code>. New convenience methods for writing content directly to a bucket path.</p> </li> <li> <p>Default methods on <code>S3.File</code>. Typed proxy interfaces extending <code>S3.File</code> now inherit default method implementations for common operations.</p> </li> <li> <p><code>jaws-s3-test</code> module. New test module with <code>MockS3</code> for local S3 simulation and <code>S3Asserts</code> for fluent JUnit 5 assertions on bucket contents.</p> </li> <li> <p>Annotations migrated into JAWS. <code>@Name</code>, <code>@Parent</code>, <code>@Walk</code>, and other annotations previously in external libraries are now part of the JAWS project directly.</p> </li> </ul>","path":["Changelog"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting Started","text":"","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#prerequisites","level":2,"title":"Prerequisites","text":"<ul> <li>Java 11 or later</li> </ul>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#installation","level":2,"title":"Installation","text":"<p>Add JAWS to your Maven project:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;org.tomitribe&lt;/groupId&gt;\n    &lt;artifactId&gt;jaws-s3&lt;/artifactId&gt;\n    &lt;version&gt;2.1.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#creating-an-s3client","level":2,"title":"Creating an S3Client","text":"<p>The <code>S3Client</code> is the entry point. It wraps an AWS <code>S3AsyncClient</code>:</p> <pre><code>import org.tomitribe.jaws.s3.S3Client;\nimport software.amazon.awssdk.services.s3.S3AsyncClient;\n\nS3AsyncClient asyncClient = S3AsyncClient.builder().build();\nS3Client s3 = new S3Client(asyncClient);\n</code></pre>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#working-with-buckets","level":2,"title":"Working with Buckets","text":"<p>Get a reference to an existing bucket:</p> <pre><code>S3Bucket bucket = s3.getBucket(\"my-bucket\");\n</code></pre> <p>Or create a new one:</p> <pre><code>S3Bucket bucket = s3.createBucket(\"my-new-bucket\");\n</code></pre> <p>List all buckets:</p> <pre><code>s3.buckets().forEach(b -&gt; System.out.println(b.getName()));\n</code></pre>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#defining-your-first-proxy","level":2,"title":"Defining Your First Proxy","text":"<p>Imagine a bucket with this structure:</p> <pre><code>my-bucket/\n  config.properties\n  users/\n    alice.json\n    bob.json\n</code></pre> <p>Define interfaces that mirror this structure:</p> <pre><code>public interface UserRepository extends S3.Dir {\n    @Name(\"config.properties\")\n    S3File config();\n\n    Users users();\n}\n\npublic interface Users extends S3.Dir {\n    Stream&lt;UserFile&gt; users();\n\n    UserFile user(String name);\n}\n\n@Match(\".*\\\\.json\")\npublic interface UserFile extends S3.File {\n    default String getUserName() {\n        return file().getName().replaceAll(\"\\\\.json$\", \"\");\n    }\n}\n</code></pre> <p>The <code>@Match(\".*\\\\.json\")</code> annotation does double duty: listing methods like <code>Stream&lt;UserFile&gt; users()</code> will only return <code>.json</code> files, and single-arg methods like <code>UserFile user(String name)</code> will reject names that don't match the pattern (throwing <code>IllegalArgumentException</code>).</p> <p>Create and use the proxy:</p> <pre><code>S3Bucket bucket = s3.getBucket(\"my-bucket\");\nUserRepository root = bucket.as(UserRepository.class);\n\n// Read config\nString config = root.config().getValueAsString();\n\n// List users\nroot.users().users().forEach(user -&gt;\n    System.out.println(user.getUserName())\n);\n\n// Get a specific user — name is validated against @Match\nUserFile alice = root.users().user(\"alice.json\");\nString json = alice.getValueAsString();\n</code></pre>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#testing","level":2,"title":"Testing","text":"<p>JAWS ships a <code>jaws-s3-test</code> module with an in-memory S3 backend powered by S3Proxy. Add it as a test dependency alongside JUnit 5:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;org.tomitribe&lt;/groupId&gt;\n    &lt;artifactId&gt;jaws-s3-test&lt;/artifactId&gt;\n    &lt;version&gt;2.1.0&lt;/version&gt;\n    &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;\n&lt;dependency&gt;\n    &lt;groupId&gt;org.junit.jupiter&lt;/groupId&gt;\n    &lt;artifactId&gt;junit-jupiter-api&lt;/artifactId&gt;\n    &lt;version&gt;5.10.2&lt;/version&gt;\n    &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;\n</code></pre> <p>Register a <code>MockS3Extension</code> in your test. It starts a local S3 server before each test and tears it down after:</p> <pre><code>import org.junit.jupiter.api.Test;\nimport org.junit.jupiter.api.extension.RegisterExtension;\nimport org.tomitribe.jaws.s3.MockS3Extension;\nimport org.tomitribe.jaws.s3.Name;\nimport org.tomitribe.jaws.s3.S3;\nimport org.tomitribe.jaws.s3.S3Asserts;\nimport org.tomitribe.jaws.s3.S3Client;\nimport org.tomitribe.jaws.s3.S3File;\n\nimport java.util.stream.Collectors;\nimport java.util.stream.Stream;\n\nimport static org.junit.jupiter.api.Assertions.assertEquals;\n\npublic class GettingStartedTest {\n\n    @RegisterExtension\n    private final MockS3Extension mockS3 = new MockS3Extension();\n\n    @Test\n    void test() {\n        final S3Client s3Client = new S3Client(mockS3.getS3Client());\n\n        final UserRepository root = s3Client.createBucket(\"my-bucket\")\n                .put(\"config.properties\", \"color=red\")\n                .put(\"users/alice.json\", \"{\\\"name\\\":\\\"Alice\\\"}\")\n                .put(\"users/bob.json\", \"{\\\"name\\\":\\\"Bob\\\"}\")\n                .as(UserRepository.class);\n\n        // Read entries\n        assertEquals(\"color=red\", root.config().getValueAsString());\n        assertEquals(\"{\\\"name\\\":\\\"Alice\\\"}\", root.users().user(\"alice.json\").getValueAsString());\n\n        // List entries\n        final String names = root.users().users()\n                .map(UserFile::getUserName)\n                .sorted()\n                .collect(Collectors.joining(\", \"));\n\n        assertEquals(\"alice, bob\", names);\n\n        // Add entry\n        root.users().user(\"charlie.json\").setValueAsString(\"{\\\"name\\\":\\\"Charlie\\\"}\");\n\n        // Review all results\n        S3Asserts.of(mockS3.getS3Client(), \"my-bucket\")\n                .snapshot()\n                .assertContent(\"users/charlie.json\", \"{\\\"name\\\":\\\"Charlie\\\"}\")\n                .assertContent(\"users/alice.json\", \"{\\\"name\\\":\\\"Alice\\\"}\")\n                .assertExists(\"config.properties\")\n                .assertNotExists(\"users/dave.json\");\n    }\n\n    public interface UserRepository extends S3.Dir {\n        @Name(\"config.properties\")\n        S3File config();\n\n        Users users();\n    }\n\n    public interface Users extends S3.Dir {\n        Stream&lt;UserFile&gt; users();\n\n        UserFile user(String name);\n    }\n\n    public interface UserFile extends S3.File {\n        default String getUserName() {\n            return file().getName().replaceAll(\"\\\\.json$\", \"\");\n        }\n    }\n}\n</code></pre> <p>The test creates a bucket, populates it with the fluent <code>put()</code> API, then uses a typed proxy to read, list, and write entries — the same code you'd write against real S3. <code>S3Asserts</code> provides a snapshot-based way to verify the final bucket state directly against the underlying S3 client.</p>","path":["Home","Getting Started"],"tags":[]},{"location":"getting-started/#next-steps","level":2,"title":"Next Steps","text":"<ul> <li>Typed Proxies — Learn the full proxy dispatch model</li> <li>Directories &amp; Files — Understand <code>S3.Dir</code> vs <code>S3.File</code></li> <li>Annotations — Fine-tune key resolution and listing behavior</li> <li>Listing &amp; Recursion — Immediate vs recursive listing with <code>@Recursive</code></li> </ul>","path":["Home","Getting Started"],"tags":[]},{"location":"api/s3-interfaces/","level":1,"title":"S3 Interfaces","text":"<p>The <code>S3</code> interface hierarchy provides the foundation for building strongly-typed proxies. Your interfaces extend one of these base types to get the appropriate behavior.</p>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#s3","level":2,"title":"S3","text":"<p>The base interface. Provides access to the backing <code>S3File</code> and parent navigation.</p> <pre><code>public interface S3 {\n    S3File file();     // the S3File backing this proxy\n    S3File parent();   // the parent S3File\n}\n</code></pre> <p>Use <code>S3</code> as the base when your interface doesn't need directory listing or file content methods.</p>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#s3dir","level":2,"title":"S3.Dir","text":"<p>Extends <code>S3</code> with directory (prefix) operations. Use this when your interface models a container of S3 objects.</p> <pre><code>public interface Dir extends S3 {\n    S3File file(String name);            // child lookup\n    Stream&lt;S3File&gt; files();              // all descendants (flat)\n    Stream&lt;S3File&gt; list();               // immediate children\n    Upload upload(File file);            // upload by filename\n    Upload upload(File file, TransferListener listener);\n}\n</code></pre>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#return-type-dispatch","level":3,"title":"Return-type dispatch","text":"<p>When a proxy method returns <code>Stream&lt;X&gt;</code> where <code>X extends S3.Dir</code>, the library uses a delimiter-based listing and returns only directories.</p> <pre><code>public interface Root extends S3.Dir {\n    Stream&lt;SubDir&gt; subdirs();  // SubDir extends S3.Dir → dirs only\n}\n</code></pre>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#s3file","level":2,"title":"S3.File","text":"<p>Extends <code>S3</code> with object content and metadata methods. Use this when your interface models a single S3 object.</p> <pre><code>public interface File extends S3 {\n    InputStream getValueAsStream();\n    String getValueAsString();\n    void setValueAsStream(InputStream is);\n    void setValueAsString(String value);\n    void setValueAsFile(java.io.File file);\n    String getETag();\n    long getSize();\n    Instant getLastModified();\n    ObjectMetadata getObjectMetadata();\n}\n</code></pre> <p>All methods are provided as defaults that delegate to the backing <code>S3File</code>.</p>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#return-type-dispatch_1","level":3,"title":"Return-type dispatch","text":"<p>When a proxy method returns <code>Stream&lt;X&gt;</code> where <code>X extends S3.File</code>, the library uses a delimiter-based listing and returns only files (not directories).</p> <pre><code>public interface Album extends S3.Dir {\n    Stream&lt;Photo&gt; photos();  // Photo extends S3.File → files only\n}\n</code></pre>","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3-interfaces/#summary","level":2,"title":"Summary","text":"Base type Use case Listing dispatch <code>S3</code> Navigation only Standard (flat) <code>S3.Dir</code> Directory / container <code>Stream&lt;X extends Dir&gt;</code> → dirs only <code>S3.File</code> Object with content <code>Stream&lt;X extends File&gt;</code> → files only","path":["API Reference","S3 Interfaces"],"tags":[]},{"location":"api/s3bucket/","level":1,"title":"S3Bucket","text":"<p>Represents a single S3 bucket and provides operations for reading, writing, listing, uploading, and downloading objects within it.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#obtaining-an-s3bucket","level":2,"title":"Obtaining an S3Bucket","text":"<pre><code>S3Bucket bucket = s3Client.getBucket(\"my-bucket\");\n// or\nS3Bucket bucket = s3Client.createBucket(\"new-bucket\");\n</code></pre>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#creating-proxies","level":2,"title":"Creating Proxies","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#as","level":3,"title":"as","text":"<pre><code>&lt;T&gt; T as(Class&lt;T&gt; clazz)\n</code></pre> <p>Creates a strongly-typed proxy for the bucket root. The given class should be an interface extending <code>S3</code>, <code>S3.Dir</code>, or <code>S3.File</code>.</p> <pre><code>Catalog catalog = bucket.as(Catalog.class);\n</code></pre>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#root","level":3,"title":"root","text":"<pre><code>S3File root()\n</code></pre> <p>Returns an <code>S3File</code> representing the bucket root directory.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#listing-objects","level":2,"title":"Listing Objects","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#objects","level":3,"title":"objects","text":"<pre><code>Stream&lt;S3File&gt; objects()\n</code></pre> <p>Returns all objects in the bucket as a flat listing.</p> <pre><code>Stream&lt;S3File&gt; objects(ListObjectsRequest request)\n</code></pre> <p>Returns objects matching the given listing request. The request's bucket is set automatically.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#reading-objects","level":2,"title":"Reading Objects","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#getobject","level":3,"title":"getObject","text":"<pre><code>ResponseInputStream&lt;GetObjectResponse&gt; getObject(String key)\n</code></pre> <p>Returns a blocking input stream for the object with the given key.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#getobjectmetadata","level":3,"title":"getObjectMetadata","text":"<pre><code>HeadObjectResponse getObjectMetadata(String key)\n</code></pre> <p>Returns the metadata for the object via a HEAD request.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#getfile","level":3,"title":"getFile","text":"<pre><code>S3File getFile(String key)\n</code></pre> <p>Returns an <code>S3File</code> for the object, pre-populated with metadata from a HEAD request.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#writing-objects","level":2,"title":"Writing Objects","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#putobject","level":3,"title":"putObject","text":"<pre><code>PutObjectResponse putObject(String key, File file)\nPutObjectResponse putObject(String key, InputStream inputStream, long contentLength)\nPutObjectResponse putObject(String key, String content)\n</code></pre> <p>Uploads content as an S3 object.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#setobjectasstring-setobjectasfile-setobjectasstream","level":3,"title":"setObjectAsString / setObjectAsFile / setObjectAsStream","text":"<pre><code>PutObjectResponse setObjectAsString(String key, String value)\nPutObjectResponse setObjectAsFile(String key, File value)\nPutObjectResponse setObjectAsStream(String key, InputStream value)\n</code></pre> <p>Replaces the content of an existing object.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#transfers","level":2,"title":"Transfers","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#upload","level":3,"title":"upload","text":"<pre><code>Upload upload(String key, File file)\nUpload upload(String key, InputStream input, long size)\n</code></pre> <p>Uploads using the S3 Transfer Manager, which handles multipart uploads automatically.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#download","level":3,"title":"download","text":"<pre><code>FileDownload download(String key, File destination)\n</code></pre> <p>Downloads an S3 object to a local file using the S3 Transfer Manager.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#other","level":2,"title":"Other","text":"","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#deleteobject","level":3,"title":"deleteObject","text":"<pre><code>void deleteObject(String key)\n</code></pre> <p>Deletes the object with the given key.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3bucket/#getname-getcreationdate-getclient","level":3,"title":"getName / getCreationDate / getClient","text":"<pre><code>String getName()\nInstant getCreationDate()\nS3Client getClient()\n</code></pre> <p>Bucket metadata accessors.</p>","path":["API Reference","S3Bucket"],"tags":[]},{"location":"api/s3client/","level":1,"title":"S3Client","text":"<p>Entry point for interacting with Amazon S3. Wraps an <code>S3AsyncClient</code> and an <code>S3TransferManager</code>, providing bucket-level operations and a shared thread pool for asynchronous transfers.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#constructor","level":2,"title":"Constructor","text":"<pre><code>S3Client(S3AsyncClient s3)\n</code></pre> <p>Creates a new <code>S3Client</code> backed by the given async client. A default thread pool and <code>S3TransferManager</code> are created automatically.</p> <p>Example:</p> <pre><code>S3AsyncClient asyncClient = S3AsyncClient.builder().build();\nS3Client s3 = new S3Client(asyncClient);\n\nS3Bucket bucket = s3.getBucket(\"my-bucket\");\nCatalog catalog = bucket.as(Catalog.class);\n</code></pre>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#methods","level":2,"title":"Methods","text":"","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#createbucket","level":3,"title":"createBucket","text":"<pre><code>S3Bucket createBucket(String name)\n</code></pre> <p>Creates a new S3 bucket with the given name and returns it.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#getbucket","level":3,"title":"getBucket","text":"<pre><code>S3Bucket getBucket(String name)\n</code></pre> <p>Returns the bucket with the given name.</p> <p>Throws: <code>NoSuchBucketException</code> if no bucket with that name exists.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#buckets","level":3,"title":"buckets","text":"<pre><code>Stream&lt;S3Bucket&gt; buckets()\n</code></pre> <p>Returns all buckets visible to the configured AWS credentials.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#gets3","level":3,"title":"getS3","text":"<pre><code>S3AsyncClient getS3()\n</code></pre> <p>Returns the underlying AWS async S3 client.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3client/#gettransfermanager","level":3,"title":"getTransferManager","text":"<pre><code>S3TransferManager getTransferManager()\n</code></pre> <p>Returns the <code>S3TransferManager</code> used for upload and download operations.</p>","path":["API Reference","S3Client"],"tags":[]},{"location":"api/s3file/","level":1,"title":"S3File","text":"<p>Represents a single S3 object or prefix (directory). <code>S3File</code> is the core abstraction in JAWS — it tracks an object's key, metadata, and state, and provides methods for reading, writing, listing, and navigating the key hierarchy.</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#obtaining-an-s3file","level":2,"title":"Obtaining an S3File","text":"<pre><code>// From a bucket\nS3File file = bucket.getFile(\"path/to/object.txt\");\nS3File root = bucket.root();\n\n// From a proxy\nS3File backing = myProxy.file();\nS3File parent = myProxy.parent();\n\n// From another S3File\nS3File child = parentFile.getFile(\"child-name\");\nS3File parent = file.getParentFile();\n</code></pre>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#creating-proxies","level":2,"title":"Creating Proxies","text":"","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#as","level":3,"title":"as","text":"<pre><code>&lt;T&gt; T as(Class&lt;T&gt; clazz)\n</code></pre> <p>Creates a strongly-typed proxy backed by this <code>S3File</code>. The given class should be an interface extending <code>S3</code>, <code>S3.Dir</code>, or <code>S3.File</code>.</p> <pre><code>S3File dir = bucket.root().getFile(\"catalog\");\nCatalog catalog = dir.as(Catalog.class);\n</code></pre>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#content-access","level":2,"title":"Content Access","text":"","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#reading","level":3,"title":"Reading","text":"<pre><code>String getValueAsString()\nInputStream getValueAsStream()\n</code></pre> <p>Read the object's content. The first call fetches the content from S3; subsequent calls may return cached content depending on the object's state.</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#writing","level":3,"title":"Writing","text":"<pre><code>void setValueAsString(String value)\nvoid setValueAsStream(InputStream is)\nvoid setValueAsFile(File file)\n</code></pre> <p>Replace the object's content. After writing, the <code>S3File</code> transitions to a state that reflects the new content.</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#metadata","level":2,"title":"Metadata","text":"<pre><code>String getETag()\nlong getSize()\nInstant getLastModified()\nObjectMetadata getObjectMetadata()\n</code></pre> <p>Returns metadata about the object. Available metadata depends on how the <code>S3File</code> was obtained (listing vs HEAD request vs content fetch).</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#navigation","level":2,"title":"Navigation","text":"<pre><code>String getName()           // the last segment of the key\nString getAbsoluteName()   // the full key\nS3File getParentFile()     // parent directory\nS3File getFile(String name) // child file\n</code></pre>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#listing","level":2,"title":"Listing","text":"<pre><code>Stream&lt;S3File&gt; files()     // all descendants (flat, no delimiter)\nStream&lt;S3File&gt; list()      // immediate children (delimiter-based)\n</code></pre>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#state","level":2,"title":"State","text":"<pre><code>boolean exists()           // HEAD request to check existence\nboolean isFile()           // true if this is an object (not a prefix)\nboolean isDirectory()      // true if this is a prefix (directory)\n</code></pre>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#uploads","level":2,"title":"Uploads","text":"<pre><code>Upload upload(File file)\nUpload upload(File file, TransferListener listener)\n</code></pre> <p>Upload a local file to this S3 location using the Transfer Manager.</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/s3file/#deletion","level":2,"title":"Deletion","text":"<pre><code>void delete()\n</code></pre> <p>Deletes the object from S3.</p>","path":["API Reference","S3File"],"tags":[]},{"location":"api/annotations/delimiter/","level":1,"title":"@Delimiter","text":"<p>Sets the delimiter for the <code>ListObjects</code> request used to query S3.</p>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Delimiter {\n    String value();\n}\n</code></pre>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#attributes","level":2,"title":"Attributes","text":"Attribute Type Description <code>value</code> <code>String</code> The delimiter character or string","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#description","level":2,"title":"Description","text":"<p>S3 uses delimiters to simulate a directory hierarchy within a flat key space. The default delimiter is <code>\"/\"</code>, which treats slashes in keys as directory separators. <code>@Delimiter</code> allows changing this for keys that use a different separator.</p> <p><code>@Delimiter</code> works on both immediate listing methods and <code>@Recursive</code> methods. When used with <code>@Recursive</code>, the delimiter controls how the walk discovers child prefixes at each level.</p>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#flat-listing-with-a-custom-delimiter","level":3,"title":"Flat listing with a custom delimiter","text":"<pre><code>public interface Logs extends S3.Dir {\n    // Keys like \"2025-01-15-access.log\", \"2025-01-15-error.log\"\n    @Delimiter(\"-\")\n    Stream&lt;S3File&gt; bySegment();\n}\n</code></pre>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#recursive-listing-with-a-custom-delimiter","level":3,"title":"Recursive listing with a custom delimiter","text":"<pre><code>public interface DateIndex extends S3.Dir {\n    // Keys like \"2025-01-15\", \"2025-02-20\"\n    // Delimiter \"-\" splits on hyphens, creating a year/month/day hierarchy\n    @Recursive\n    @Delimiter(\"-\")\n    Stream&lt;S3.Dir&gt; byDate();\n}\n</code></pre>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#no-delimiter","level":3,"title":"No delimiter","text":"<p>An empty string disables delimiter-based grouping entirely:</p> <pre><code>public interface Bucket extends S3.Dir {\n    // Returns all keys as a flat list, no directory grouping\n    @Delimiter(\"\")\n    Stream&lt;S3File&gt; allKeys();\n}\n</code></pre>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/delimiter/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Recursive — recursive listing (defaults to <code>\"/\"</code> delimiter for S3.Dir types)</li> <li>Listing &amp; Recursion — how delimiters affect listings</li> </ul>","path":["API Reference","Annotations","@Delimiter"],"tags":[]},{"location":"api/annotations/filter/","level":1,"title":"@Filter","text":"<p>Applies a client-side predicate to filter listing results.</p>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Repeatable(Filters.class)\n@Target({ElementType.METHOD, ElementType.TYPE})\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Filter {\n    Class&lt;? extends Predicate&lt;S3File&gt;&gt; value();\n}\n</code></pre>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#attributes","level":2,"title":"Attributes","text":"Attribute Type Description <code>value</code> <code>Class&lt;? extends Predicate&lt;S3File&gt;&gt;</code> The predicate class to use as a filter. Must have a no-arg constructor.","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#description","level":2,"title":"Description","text":"<p>Applies an arbitrary client-side predicate to filter listing results. The predicate class is instantiated via its no-arg constructor and applied to each <code>S3File</code> in the listing.</p> <p><code>@Filter</code> is the most powerful filtering annotation, but also the last to run. When a method or type carries several filter annotations, they are evaluated in a fixed order — simplest and cheapest first:</p> <ol> <li>@Prefix — server-side, in the <code>ListObjects</code> request</li> <li>@Suffix includes — client-side, <code>String.endsWith()</code></li> <li>@Suffix excludes</li> <li>@Match includes — client-side, compiled regex</li> <li>@Match excludes</li> <li>@Filter — client-side, arbitrary <code>Predicate&lt;S3File&gt;</code></li> </ol> <p>Because <code>@Filter</code> runs last, the predicate will only see entries that have already passed <code>@Prefix</code>, <code>@Suffix</code>, and <code>@Match</code>. This is intentional — a predicate can safely assume, for example, that the file name ends with <code>.jar</code> if <code>@Suffix(\".jar\")</code> is also present on the same method or type.</p> <p><code>@Filter</code> is repeatable — multiple filters on the same method are combined with AND logic. It can be placed on methods or on interface types. Interface-level filters run before method-level filters.</p> <p>Tip</p> <p>When possible, use a simpler annotation. <code>@Prefix</code> filters server-side, reducing the HTTP payload from AWS. <code>@Suffix</code> handles extension checks and <code>@Match</code> handles name patterns. Reserve <code>@Filter</code> for criteria that need access to the full <code>S3File</code> — size, metadata, path components, or other attributes beyond the name.</p>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#method-level-filter","level":3,"title":"Method-level filter","text":"<pre><code>public class IsJar implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".jar\");\n    }\n}\n\npublic class IsSnapshot implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().contains(\"SNAPSHOT\");\n    }\n}\n\npublic interface VersionDir extends S3.Dir {\n    @Filter(IsJar.class)\n    Stream&lt;S3File&gt; jars();\n\n    @Filter(IsSnapshot.class)\n    Stream&lt;S3File&gt; snapshots();\n\n    // Multiple filters — both must pass (AND logic)\n    @Filter(IsJar.class)\n    @Filter(IsSnapshot.class)\n    Stream&lt;S3File&gt; snapshotJars();\n}\n</code></pre>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#type-level-filter","level":3,"title":"Type-level filter","text":"<p>When <code>@Filter</code> is placed on an interface, it applies automatically to every listing method that returns that type:</p> <pre><code>@Filter(IsJar.class)\npublic interface JarFile extends S3.File {\n    // Any Stream&lt;JarFile&gt; listing will only include .jar files\n}\n\npublic interface VersionDir extends S3.Dir {\n    Stream&lt;JarFile&gt; artifacts();   // automatically filtered to .jar files\n    Stream&lt;S3File&gt; everything();   // no filter applied\n}\n</code></pre>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#what-your-predicate-sees","level":3,"title":"What your predicate sees","text":"<p>Because <code>@Prefix</code>, <code>@Suffix</code>, and <code>@Match</code> all run before <code>@Filter</code>, you can write predicates that depend on those earlier filters having already narrowed the results:</p> <pre><code>// This predicate can safely assume every file ends with \".jar\"\n// because @Suffix(\".jar\") already ran.  It strips the suffix\n// and inspects what remains.\npublic class IsSnapshot implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        String name = file.getName();                     // e.g. \"commons-lang-3.14-SNAPSHOT.jar\"\n        String base = name.substring(0, name.length() - 4); // strip \".jar\"\n        return base.endsWith(\"-SNAPSHOT\");\n    }\n}\n\n@Suffix(\".jar\")\npublic interface JarFile extends S3.File {}\n\npublic interface VersionDir extends S3.Dir {\n    // @Suffix(\".jar\") on JarFile runs first\n    // @Filter(IsSnapshot.class) sees only .jar files\n    @Filter(IsSnapshot.class)\n    Stream&lt;JarFile&gt; snapshotJars();\n}\n</code></pre>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#input-validation-on-single-arg-methods","level":3,"title":"Input validation on single-arg methods","text":"<p>When <code>@Filter</code> is placed on the return type of a single-arg proxy method, it validates the input name. If the <code>S3File</code> doesn't pass the predicate, an <code>IllegalArgumentException</code> is thrown:</p> <pre><code>@Filter(IsJar.class)\npublic interface JarFile extends S3.File {}\n\npublic interface VersionDir extends S3.Dir {\n    Stream&lt;JarFile&gt; artifacts();         // lists only .jar files\n    JarFile artifact(String name);       // validates name passes IsJar\n}\n\nversionDir.artifact(\"commons.jar\");      // OK\nversionDir.artifact(\"readme.txt\");       // throws IllegalArgumentException\n</code></pre> <p>This ensures that names passed to single-arg methods are consistent with the filtering applied to listing methods that return the same type.</p>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#combining-with-other-annotations","level":3,"title":"Combining with other annotations","text":"<p>For maximum efficiency, use <code>@Prefix</code> to reduce the result set server-side, then layer on client-side filters as needed:</p> <pre><code>public interface Repository extends S3.Dir {\n    @Prefix(\"org/apache\")\n    @Suffix(\".jar\")\n    Stream&lt;S3File&gt; apacheJars();\n\n    @Prefix(\"org/apache\")\n    @Suffix(\".jar\")\n    @Filter(IsSnapshot.class)\n    Stream&lt;S3File&gt; apacheSnapshotJars();\n}\n</code></pre>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/filter/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Suffix — client-side suffix filtering (simpler, faster)</li> <li>@Match — client-side regex filtering</li> <li>@Prefix — server-side prefix filtering</li> <li>Filtering — detailed guide on filtering strategies</li> </ul>","path":["API Reference","Annotations","@Filter"],"tags":[]},{"location":"api/annotations/marker/","level":1,"title":"@Marker","text":"<p>Sets the starting position for a listing in the <code>ListObjects</code> request.</p>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Marker {\n    String value();\n}\n</code></pre>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#attributes","level":2,"title":"Attributes","text":"Attribute Type Description <code>value</code> <code>String</code> The key to start listing from (exclusive)","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#description","level":2,"title":"Description","text":"<p>The marker indicates where in the bucket to begin listing. The list will only include keys that occur lexicographically after the marker. This is the low-level pagination mechanism used by S3.</p> <p>In most cases, JAWS handles pagination automatically — truncated responses are followed up with additional requests using the <code>nextMarker</code> from the previous response. <code>@Marker</code> is useful when you need to start a listing from a known position, such as resuming a previously interrupted scan.</p>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#skip-to-a-known-position","level":3,"title":"Skip to a known position","text":"<pre><code>public interface Archive extends S3.Dir {\n    @Marker(\"2024-01-01\")\n    Stream&lt;S3File&gt; since2024();\n}\n</code></pre>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#combine-with-prefix","level":3,"title":"Combine with @Prefix","text":"<pre><code>public interface Logs extends S3.Dir {\n    // Only keys starting with \"access-\" and after \"access-2024-06\"\n    @Prefix(\"access-\")\n    @Marker(\"access-2024-06\")\n    Stream&lt;S3File&gt; recentAccessLogs();\n}\n</code></pre> <p>Note</p> <p>The marker value is compared lexicographically against the full S3 key. Results begin with the first key that is strictly greater than the marker.</p>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/marker/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Prefix — narrow results by key prefix</li> <li>@Delimiter — control directory grouping</li> </ul>","path":["API Reference","Annotations","@Marker"],"tags":[]},{"location":"api/annotations/match/","level":1,"title":"@Match","text":"<p>Filters listing results to entries whose name matches a regular expression.</p>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Repeatable(Matches.class)\n@Target({ElementType.METHOD, ElementType.TYPE})\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Match {\n    String value();\n    boolean exclude() default false;\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#attributes","level":2,"title":"Attributes","text":"Attribute Type Default Description <code>value</code> <code>String</code> — A regular expression matched against <code>S3File.getName()</code> <code>exclude</code> <code>boolean</code> <code>false</code> When <code>true</code>, entries matching the pattern are excluded instead of included","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#description","level":2,"title":"Description","text":"<p>Filters results on the client side using <code>Pattern.compile(value).asMatchPredicate()</code>. The entire file name must match the regex (implied <code>^</code> and <code>$</code>) — this is a full match, not a substring search.</p> <p><code>@Match</code> can be placed on methods or on interface types. It is applied after <code>@Suffix</code> and before <code>@Filter</code> in the evaluation order.</p> <p><code>@Match</code> is repeatable. Include annotations (default) are applied first, then exclude annotations carve out exceptions. This lets you select a broad set and remove specific entries:</p> <pre><code>// All CSS files except reset.css\n@Match(\".*\\\\.css\")\n@Match(value = \"reset\\\\.css\", exclude = true)\nStream&lt;S3File&gt; cssExceptReset();\n</code></pre> <p>Use <code>@Match</code> when the pattern is more complex than a simple suffix. For suffix-only filtering, prefer <code>@Suffix</code> — it is cheaper and easier to read.</p>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#basic-regex","level":3,"title":"Basic regex","text":"<pre><code>public interface Reports extends S3.Dir {\n    // Match files like daily-2025-01-15.csv, daily-2025-02-01.csv\n    @Match(\"daily-\\\\d{4}-\\\\d{2}-\\\\d{2}\\\\.csv\")\n    Stream&lt;S3File&gt; dailyReports();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#alternation","level":3,"title":"Alternation","text":"<p>Use regex alternation to match multiple extensions:</p> <pre><code>public interface Assets extends S3.Dir {\n    @Match(\".*\\\\.(jpg|png|gif|webp)\")\n    Stream&lt;S3File&gt; images();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#full-match-semantics","level":3,"title":"Full-match semantics","text":"<p>The regex must match the entire file name. A partial pattern will not match:</p> <pre><code>public interface Assets extends S3.Dir {\n    @Match(\"css\")          // matches nothing — no file is named exactly \"css\"\n    Stream&lt;S3File&gt; broken();\n\n    @Match(\".*\\\\.css\")     // matches main.css, reset.css, etc.\n    Stream&lt;S3File&gt; correct();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#exclude","level":3,"title":"Exclude","text":"<p>Use <code>exclude = true</code> to remove entries that match a pattern:</p> <pre><code>public interface Groups extends S3.Dir {\n    // All directories except those ending in -alpha\n    @Match(value = \".*-alpha\", exclude = true)\n    Stream&lt;S3.Dir&gt; nonAlpha();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#include-exclude","level":3,"title":"Include + Exclude","text":"<p>Combine include and exclude annotations on the same method:</p> <pre><code>public interface Assets extends S3.Dir {\n    // All CSS files except reset.css\n    @Match(\".*\\\\.css\")\n    @Match(value = \"reset\\\\.css\", exclude = true)\n    Stream&lt;S3File&gt; cssExceptReset();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#type-level-match","level":3,"title":"Type-level match","text":"<p>When <code>@Match</code> is placed on an interface, it applies automatically to every listing method that returns that type:</p> <pre><code>@Match(\"\\\\d{4}-\\\\d{2}-\\\\d{2}\\\\.csv\")\npublic interface DailyReport extends S3.File {\n    // Any Stream&lt;DailyReport&gt; listing will only include\n    // files matching the date-based CSV pattern\n}\n\npublic interface Reports extends S3.Dir {\n    Stream&lt;DailyReport&gt; reports();   // automatically filtered\n    Stream&lt;S3File&gt; everything();     // no filter applied\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#input-validation-on-single-arg-methods","level":3,"title":"Input validation on single-arg methods","text":"<p>When <code>@Match</code> is placed on the return type of a single-arg proxy method, it validates the input name. If the name doesn't match the pattern, an <code>IllegalArgumentException</code> is thrown:</p> <pre><code>@Match(\".*\\\\.json\")\npublic interface UserFile extends S3.File {}\n\npublic interface Users extends S3.Dir {\n    Stream&lt;UserFile&gt; users();       // lists only .json files\n    UserFile user(String name);     // validates name matches .*\\.json\n}\n\nusers.user(\"alice.json\");     // OK — matches the pattern\nusers.user(\"notes.txt\");      // throws IllegalArgumentException\n</code></pre> <p>This ensures that names passed to single-arg methods are consistent with the filtering applied to listing methods that return the same type.</p> <p><code>@Match</code> on the method itself also validates:</p> <pre><code>public interface Reports extends S3.Dir {\n    @Match(\"report-\\\\d{4}\\\\.csv\")\n    S3File report(String name);     // validates name matches the pattern\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#combining-with-other-annotations","level":3,"title":"Combining with other annotations","text":"<p><code>@Match</code> can be combined with <code>@Prefix</code>, <code>@Suffix</code>, and <code>@Filter</code>. They are evaluated in order — each filter only sees entries that passed the previous one:</p> <pre><code>public interface DataDir extends S3.Dir {\n    // 1. @Prefix server-side: keys starting with \"export-\"\n    // 2. @Suffix client-side: names ending with \".parquet\"\n    // 3. @Match client-side: only 2025 exports\n    @Prefix(\"export-\")\n    @Suffix(\".parquet\")\n    @Match(\"export-2025-.*\\\\.parquet\")\n    Stream&lt;S3File&gt; exports2025();\n}\n</code></pre>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#evaluation-order","level":2,"title":"Evaluation Order","text":"<p>When multiple filter annotations are present on the same method or type, they are applied in this order:</p> <ol> <li>@Prefix — server-side, in the <code>ListObjects</code> request</li> <li>@Suffix includes — client-side, <code>String.endsWith()</code></li> <li>@Suffix excludes</li> <li>@Match includes — client-side, regex</li> <li>@Match excludes</li> <li>@Filter — client-side, arbitrary <code>Predicate&lt;S3File&gt;</code></li> </ol> <p>All client-side filters are AND'd together. A <code>@Filter</code> predicate can safely assume that <code>@Suffix</code> and <code>@Match</code> have already passed.</p>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/match/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Suffix — client-side suffix filtering (simpler, faster)</li> <li>@Filter — client-side filtering with arbitrary predicates</li> <li>@Prefix — server-side prefix filtering</li> <li>Filtering — detailed guide on filtering strategies</li> </ul>","path":["API Reference","Annotations","@Match"],"tags":[]},{"location":"api/annotations/name/","level":1,"title":"@Name","text":"<p>Overrides the S3 key segment used when resolving a proxy method to a child object.</p>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\npublic @interface Name {\n    String value();\n}\n</code></pre>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#attributes","level":2,"title":"Attributes","text":"Attribute Type Description <code>value</code> <code>String</code> The S3 key segment to use instead of the method name","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#description","level":2,"title":"Description","text":"<p>By default, the proxy handler uses the method name as the S3 key segment when looking up a child object. <code>@Name</code> allows mapping to an S3 key that cannot be expressed as a valid Java identifier — for example, keys containing dots, hyphens, or slashes.</p>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#mapping-to-a-dotted-filename","level":3,"title":"Mapping to a dotted filename","text":"<pre><code>public interface Module extends S3.Dir {\n    @Name(\"pom.xml\")\n    PomFile pomXml();\n}\n</code></pre> <p>Without <code>@Name</code>, <code>pomXml()</code> would resolve to the key segment <code>pomXml</code>. With <code>@Name(\"pom.xml\")</code>, it resolves to <code>pom.xml</code>.</p>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#mapping-to-a-path-with-slashes","level":3,"title":"Mapping to a path with slashes","text":"<pre><code>public interface Project extends S3.Dir {\n    @Name(\"src/main/java\")\n    Sources sources();\n}\n</code></pre>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#mapping-to-a-hyphenated-name","level":3,"title":"Mapping to a hyphenated name","text":"<pre><code>public interface Catalog extends S3.Dir {\n    @Name(\"my-config\")\n    Config myConfig();\n\n    @Name(\"release-notes.txt\")\n    ReleaseNotes releaseNotes();\n}\n</code></pre>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/name/#see-also","level":2,"title":"See Also","text":"<ul> <li>Typed Proxies — how method names map to S3 keys</li> <li>@Parent — navigate upward instead of to a child</li> </ul>","path":["API Reference","Annotations","@Name"],"tags":[]},{"location":"api/annotations/parent/","level":1,"title":"@Parent","text":"<p>Navigates upward in the S3 key hierarchy from the current proxy's position.</p>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\npublic @interface Parent {\n    int value() default 1;\n}\n</code></pre>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#attributes","level":2,"title":"Attributes","text":"Attribute Type Default Description <code>value</code> <code>int</code> <code>1</code> The number of levels to navigate upward","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#description","level":2,"title":"Description","text":"<p>Marks a proxy method as navigating upward rather than downward. The annotated method must return an interface extending <code>S3</code>, and the proxy will be created for the ancestor at the specified depth.</p> <p>A value of <code>1</code> (the default) navigates to the immediate parent. Higher values skip intermediate levels.</p>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#navigate-to-immediate-parent","level":3,"title":"Navigate to immediate parent","text":"<pre><code>public interface Version extends S3.Dir {\n    @Parent\n    ArtifactDir artifact();\n}\n</code></pre> <p>Given a proxy at <code>org/apache/maven/maven-core/3.9.6/</code>, calling <code>artifact()</code> returns a proxy at <code>org/apache/maven/maven-core/</code>.</p>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#navigate-multiple-levels","level":3,"title":"Navigate multiple levels","text":"<pre><code>public interface Version extends S3.Dir {\n    // version → artifact (1 level)\n    @Parent\n    ArtifactDir artifact();\n\n    // version → artifact → group (2 levels)\n    @Parent(2)\n    GroupDir group();\n\n    // version → artifact → group → repository root (3 levels)\n    @Parent(3)\n    Repository repository();\n}\n</code></pre>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#practical-use-case-breadcrumb-navigation","level":3,"title":"Practical use case — breadcrumb navigation","text":"<pre><code>public interface Photo extends S3.File {\n    @Parent\n    Album album();\n\n    @Parent(2)\n    Year year();\n\n    @Parent(3)\n    PhotoLibrary library();\n}\n\n// Navigate from a deeply nested photo back to its album\nPhoto photo = library.year(\"2024\").album(\"vacation\").photo(\"sunset.jpg\");\nAlbum album = photo.album();          // one level up\nYear year = photo.year();             // two levels up\nPhotoLibrary lib = photo.library();   // three levels up\n</code></pre>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#errors","level":2,"title":"Errors","text":"<p>A <code>NoParentException</code> is thrown at runtime if the proxy is already at the bucket root and no further parent exists.</p>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/parent/#see-also","level":2,"title":"See Also","text":"<ul> <li>Typed Proxies — proxy dispatch model</li> <li>@Name — navigate downward to a named child</li> </ul>","path":["API Reference","Annotations","@Parent"],"tags":[]},{"location":"api/annotations/prefix/","level":1,"title":"@Prefix","text":"<p>Narrows a listing or walk to only S3 keys that begin with the given prefix.</p>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Prefix {\n    String value();\n}\n</code></pre>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#attributes","level":2,"title":"Attributes","text":"Attribute Type Description <code>value</code> <code>String</code> The prefix to filter by, relative to the current proxy's position","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#description","level":2,"title":"Description","text":"<p>The prefix value is relative — it is appended to the current proxy's position in the key hierarchy. The prefix is sent server-side in the <code>ListObjects</code> request, so keys that do not match are never returned by AWS and never consume bandwidth or processing.</p> <p><code>@Prefix</code> works on both immediate listing methods and <code>@Recursive</code> methods.</p>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#examples","level":2,"title":"Examples","text":"<p>Consider a Maven repository stored in S3:</p> <pre><code>repository/\n  org/apache/maven/maven-core/3.9.6/maven-core-3.9.6.jar\n  org/apache/maven/maven-core/3.9.6/maven-core-3.9.6.pom\n  org/apache/tomcat/tomcat/10.1.18/tomcat-10.1.18.jar\n  com/google/guava/guava/33.0/guava-33.0.jar\n  junit/junit/4.13.2/junit-4.13.2.jar\n</code></pre>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#flat-listing","level":3,"title":"Flat listing","text":"<p>On a method without <code>@Recursive</code>, the prefix narrows an immediate listing:</p> <pre><code>public interface Repository extends S3.Dir {\n    @Prefix(\"org/apache\")\n    Stream&lt;S3File&gt; apacheArtifacts();\n}\n</code></pre>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#recursive-listing","level":3,"title":"Recursive listing","text":"<p>Combined with <code>@Recursive</code>, the prefix limits which keys are returned, reducing both the bandwidth and the volume of results:</p> <pre><code>public interface Repository extends S3.Dir {\n    // Recursively list only keys under the org/ namespace\n    // — com/ and junit/ are never fetched from AWS\n    @Recursive\n    @Prefix(\"org/\")\n    Stream&lt;S3File&gt; orgArtifacts();\n}\n</code></pre> <p>Without <code>@Prefix</code>, the recursive listing returns every key in the directory. With <code>@Prefix(\"org/\")</code>, only keys under <code>org/</code> are returned by AWS. In a large repository with thousands of groupIds, this can significantly reduce the data transferred.</p>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#partial-prefixes","level":3,"title":"Partial prefixes","text":"<p>The prefix does not need to end with a delimiter — it can be any key prefix, including a partial name. This is useful for matching a subset of entries within a single directory:</p> <pre><code>public interface Tomcat extends S3.Dir {\n    // Only version directories starting with \"10.\"\n    // Matches 10.1.18/, 10.1.19/, etc.\n    // Skips 9.0.85/, 11.0.0/, etc.\n    @Prefix(\"10.\")\n    Stream&lt;S3File&gt; version10();\n}\n\nS3File tomcatDir = bucket.getFile(\"org/apache/tomcat/tomcat\");\nTomcat tomcat = tomcatDir.as(Tomcat.class);\ntomcat.version10().forEach(v -&gt; System.out.println(v.getName()));\n</code></pre> <p>Because the prefix is applied server-side, AWS returns only the matching keys. A directory with hundreds of version entries can be efficiently narrowed to just the relevant subset.</p>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#input-validation-on-single-arg-methods","level":2,"title":"Input Validation on Single-Arg Methods","text":"<p>When <code>@Prefix</code> is placed on a single-arg proxy method, it validates that the input name starts with the prefix. If not, an <code>IllegalArgumentException</code> is thrown:</p> <pre><code>public interface Logs extends S3.Dir {\n    @Prefix(\"app-\")\n    Stream&lt;S3File&gt; appLogs();            // server-side prefix filter\n\n    @Prefix(\"app-\")\n    S3File appLog(String name);          // validates name starts with \"app-\"\n}\n\nlogs.appLog(\"app-2025-03-01.log\");       // OK\nlogs.appLog(\"system-2025-03-01.log\");    // throws IllegalArgumentException\n</code></pre> <p>Note that <code>@Prefix</code> is method-only (<code>@Target(ElementType.METHOD)</code>), so it cannot be placed on a type. For listing methods, the prefix is applied server-side in the <code>ListObjects</code> request. For single-arg methods, it is checked client-side against the input name.</p>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/prefix/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Recursive — recursive listing</li> <li>@Suffix — client-side suffix filtering</li> <li>@Match — client-side regex filtering</li> <li>@Filter — client-side filtering with arbitrary predicates</li> <li>Filtering — server-side vs client-side filtering</li> </ul>","path":["API Reference","Annotations","@Prefix"],"tags":[]},{"location":"api/annotations/recursive/","level":1,"title":"@Recursive","text":"<p>Marks a listing method as recursive — all descendants are returned, not just immediate children.</p>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Target(ElementType.METHOD)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\npublic @interface Recursive {\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#description","level":2,"title":"Description","text":"<p>Without <code>@Recursive</code>, all listing methods use a delimiter-based listing that returns only immediate children at one level. With <code>@Recursive</code>, the listing returns all descendants.</p> <p>The behavior depends on the element type:</p> <ul> <li><code>Stream&lt;S3File&gt;</code> — uses <code>files()</code>, a single flat request returning all   descendant objects</li> <li><code>Stream&lt;X extends S3.Dir&gt;</code> — uses <code>walk()</code>, a multi-request tree   traversal returning directories only</li> </ul> <p>If the element type extends <code>S3.File</code>, omit <code>@Recursive</code> for best performance. A recursive listing already returns only files.</p> <p>There is no depth control. For depth-limited traversal, use the <code>S3File.walk()</code> methods directly.</p>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#recursive-listing-of-all-objects","level":3,"title":"Recursive listing of all objects","text":"<pre><code>public interface Repository extends S3.Dir {\n    @Recursive\n    Stream&lt;S3File&gt; everything();\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#recursive-listing-of-directories","level":3,"title":"Recursive listing of directories","text":"<pre><code>public interface Deployment extends S3.Dir {\n    @Recursive\n    Stream&lt;S3.Dir&gt; layout();\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#recursive-listing-with-prefix-narrowing","level":3,"title":"Recursive listing with prefix narrowing","text":"<pre><code>public interface Repository extends S3.Dir {\n    // Recursively list only keys under the org/ namespace\n    // — com/ and junit/ are never fetched from AWS\n    @Recursive\n    @Prefix(\"org/\")\n    Stream&lt;S3File&gt; orgArtifacts();\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#recursive-listing-with-filter","level":3,"title":"Recursive listing with filter","text":"<pre><code>public interface Processed extends S3.Dir {\n    @Recursive\n    @Filter(IsParquet.class)\n    Stream&lt;S3File&gt; allParquetFiles();\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#recursive-listing-with-custom-delimiter","level":3,"title":"Recursive listing with custom delimiter","text":"<pre><code>public interface DateIndex extends S3.Dir {\n    // Keys like \"2025-01-15\", \"2025-02-20\"\n    @Recursive\n    @Delimiter(\"-\")\n    Stream&lt;S3.Dir&gt; byDate();\n}\n</code></pre>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#aws-request-cost","level":2,"title":"AWS Request Cost","text":"<p>For <code>@Recursive Stream&lt;S3File&gt;</code> (and other non-<code>S3.Dir</code> types), the listing uses a single flat <code>ListObjects</code> request (no delimiter) plus pagination if needed. This is the most efficient way to get all objects.</p> <p>For <code>@Recursive Stream&lt;X extends S3.Dir&gt;</code>, each prefix visited generates at least one <code>ListObjects</code> request. If a single response is truncated (more than 1,000 keys), additional paginated requests are made automatically.</p>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/recursive/#see-also","level":2,"title":"See Also","text":"<ul> <li>Listing &amp; Recursion — detailed walkthrough</li> <li>@Prefix — narrow which keys are returned</li> <li>@Delimiter — change how the hierarchy is split</li> </ul>","path":["API Reference","Annotations","@Recursive"],"tags":[]},{"location":"api/annotations/suffix/","level":1,"title":"@Suffix","text":"<p>Filters listing results to entries whose name ends with one of the given suffixes.</p>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#declaration","level":2,"title":"Declaration","text":"<pre><code>@Repeatable(Suffixes.class)\n@Target({ElementType.METHOD, ElementType.TYPE})\n@Retention(RetentionPolicy.RUNTIME)\npublic @interface Suffix {\n    String[] value();\n    boolean exclude() default false;\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#attributes","level":2,"title":"Attributes","text":"Attribute Type Default Description <code>value</code> <code>String[]</code> — One or more suffixes to match against <code>S3File.getName()</code> <code>exclude</code> <code>boolean</code> <code>false</code> When <code>true</code>, entries matching any suffix are excluded instead of included","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#description","level":2,"title":"Description","text":"<p>Filters results on the client side using <code>String.endsWith()</code>. When multiple suffixes are given, they are OR'd — a file matches if its name ends with any of them.</p> <p><code>@Suffix</code> can be placed on methods or on interface types. It is applied before <code>@Match</code> and <code>@Filter</code> in the evaluation order.</p> <p><code>@Suffix</code> is repeatable. Include annotations (default) are applied first, then exclude annotations carve out exceptions. This lets you select a broad set and remove specific entries:</p> <pre><code>// All .jar files except sources and javadoc jars\n@Suffix(\".jar\")\n@Suffix(value = {\"-sources.jar\", \"-javadoc.jar\"}, exclude = true)\nStream&lt;S3File&gt; binaryJars();\n</code></pre> <p>This is a shorthand for the common case of filtering by file extension. For more complex name patterns, use <code>@Match</code>. For arbitrary criteria, use <code>@Filter</code>.</p>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#examples","level":2,"title":"Examples","text":"","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#single-suffix","level":3,"title":"Single suffix","text":"<pre><code>public interface Assets extends S3.Dir {\n    @Suffix(\".css\")\n    Stream&lt;S3File&gt; stylesheets();\n\n    @Suffix(\".js\")\n    Stream&lt;S3File&gt; scripts();\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#multiple-suffixes-or","level":3,"title":"Multiple suffixes (OR)","text":"<p>Multiple values match if the name ends with any of them:</p> <pre><code>public interface Assets extends S3.Dir {\n    @Suffix({\".jpg\", \".png\", \".gif\"})\n    Stream&lt;S3File&gt; images();\n\n    @Suffix({\".html\", \".htm\"})\n    Stream&lt;S3File&gt; pages();\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#exclude","level":3,"title":"Exclude","text":"<p>Use <code>exclude = true</code> to remove entries that match a suffix:</p> <pre><code>public interface Assets extends S3.Dir {\n    // Everything except JavaScript files\n    @Suffix(value = \".js\", exclude = true)\n    Stream&lt;S3File&gt; noJavaScript();\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#include-exclude","level":3,"title":"Include + Exclude","text":"<p>Combine include and exclude annotations on the same method:</p> <pre><code>public interface Assets extends S3.Dir {\n    // All CSS files except reset.css\n    @Suffix(\".css\")\n    @Suffix(value = \"reset.css\", exclude = true)\n    Stream&lt;S3File&gt; cssExceptReset();\n\n    // All .jar files except sources and javadoc jars\n    @Suffix(\".jar\")\n    @Suffix(value = {\"-sources.jar\", \"-javadoc.jar\"}, exclude = true)\n    Stream&lt;S3File&gt; binaryJars();\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#type-level-suffix","level":3,"title":"Type-level suffix","text":"<p>When <code>@Suffix</code> is placed on an interface, it applies automatically to every listing method that returns that type:</p> <pre><code>@Suffix(\".parquet\")\npublic interface ParquetFile extends S3.File {\n    // Any Stream&lt;ParquetFile&gt; listing will only include .parquet files\n}\n\npublic interface DataDir extends S3.Dir {\n    Stream&lt;ParquetFile&gt; data();    // automatically filtered to .parquet files\n    Stream&lt;S3File&gt; everything();   // no filter applied\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#input-validation-on-single-arg-methods","level":3,"title":"Input validation on single-arg methods","text":"<p>When <code>@Suffix</code> is placed on the return type of a single-arg proxy method, it validates the input name. If the name doesn't end with the required suffix, an <code>IllegalArgumentException</code> is thrown:</p> <pre><code>@Suffix(\".parquet\")\npublic interface ParquetFile extends S3.File {}\n\npublic interface DataDir extends S3.Dir {\n    Stream&lt;ParquetFile&gt; data();           // lists only .parquet files\n    ParquetFile partition(String name);   // validates name ends with .parquet\n}\n\ndataDir.partition(\"2025-01.parquet\");   // OK\ndataDir.partition(\"2025-01.csv\");       // throws IllegalArgumentException\n</code></pre> <p>This ensures that names passed to single-arg methods are consistent with the filtering applied to listing methods that return the same type.</p>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#combining-with-other-annotations","level":3,"title":"Combining with other annotations","text":"<p><code>@Suffix</code> can be combined with <code>@Prefix</code>, <code>@Match</code>, and <code>@Filter</code>. They are evaluated in order — each filter only sees entries that passed the previous one:</p> <pre><code>public interface LogDir extends S3.Dir {\n    // 1. @Prefix server-side: keys starting with \"app-\"\n    // 2. @Suffix client-side: names ending with \".log\"\n    @Prefix(\"app-\")\n    @Suffix(\".log\")\n    Stream&lt;S3File&gt; appLogs();\n}\n</code></pre>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#evaluation-order","level":2,"title":"Evaluation Order","text":"<p>When multiple filter annotations are present on the same method or type, they are applied in this order:</p> <ol> <li>@Prefix — server-side, in the <code>ListObjects</code> request</li> <li>@Suffix includes — client-side, <code>String.endsWith()</code></li> <li>@Suffix excludes</li> <li>@Match includes — client-side, regex</li> <li>@Match excludes</li> <li>@Filter — client-side, arbitrary <code>Predicate&lt;S3File&gt;</code></li> </ol> <p>All client-side filters are AND'd together. A <code>@Filter</code> predicate can safely assume that <code>@Suffix</code> and <code>@Match</code> have already passed.</p>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"api/annotations/suffix/#see-also","level":2,"title":"See Also","text":"<ul> <li>@Match — client-side regex filtering</li> <li>@Filter — client-side filtering with arbitrary predicates</li> <li>@Prefix — server-side prefix filtering</li> <li>Filtering — detailed guide on filtering strategies</li> </ul>","path":["API Reference","Annotations","@Suffix"],"tags":[]},{"location":"examples/container-registry/","level":1,"title":"Container Registry","text":"<p>This example models an OCI/Docker container image registry stored in S3. It demonstrates deep interface chaining, <code>@Name</code> for underscore-prefixed keys, <code>getValueAsStream()</code> for binary data, and default methods with domain logic.</p>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#s3-bucket-structure","level":2,"title":"S3 Bucket Structure","text":"<pre><code>v2/\n  repositories/\n    myorg/\n      api-server/\n        _manifests/\n          tags/\n            latest/\n              current/\n                link\n            v1.0.0/\n              current/\n                link\n            v1.1.0/\n              current/\n                link\n          revisions/\n            sha256/\n              abc123.../\n                link\n        _layers/\n          sha256/\n            def456.../\n              link\n            789abc.../\n              link\n      web-frontend/\n        _manifests/\n          tags/\n            latest/\n              current/\n                link\n        _layers/\n          sha256/\n            aaa111.../\n              link\n    blobs/\n      sha256/\n        abc123.../\n          data\n        def456.../\n          data\n        789abc.../\n          data\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#interfaces","level":2,"title":"Interfaces","text":"<pre><code>// Registry root — the v2/ prefix\npublic interface Registry extends S3.Dir {\n    Repositories repositories();\n    BlobStore blobs();\n}\n\n// The repositories/ directory\npublic interface Repositories extends S3.Dir {\n    // Navigate to a namespace (e.g., \"myorg\")\n    Namespace namespace(String name);\n    Stream&lt;Namespace&gt; namespaces();\n}\n\n// A namespace (organization or user)\npublic interface Namespace extends S3.Dir {\n    ImageRepo image(String name);\n    Stream&lt;ImageRepo&gt; images();\n}\n\n// A single image repository (e.g., api-server)\npublic interface ImageRepo extends S3.Dir {\n\n    // @Name handles the underscore-prefixed directory names\n    @Name(\"_manifests\")\n    Manifests manifests();\n\n    @Name(\"_layers\")\n    Layers layers();\n}\n\n// The _manifests/ directory\npublic interface Manifests extends S3.Dir {\n    Tags tags();\n    Revisions revisions();\n}\n\n// The tags/ directory — lists all available tags\npublic interface Tags extends S3.Dir {\n    Tag tag(String name);\n    Stream&lt;Tag&gt; tags();\n}\n\n// A single tag (e.g., \"latest\", \"v1.0.0\")\npublic interface Tag extends S3.Dir {\n    Current current();\n}\n\n// The current/ subdirectory under a tag\npublic interface Current extends S3.Dir {\n    LinkFile link();\n}\n\n// The revisions/ directory\npublic interface Revisions extends S3.Dir {\n    DigestDir sha256();\n}\n\n// The _layers/ directory\npublic interface Layers extends S3.Dir {\n    DigestDir sha256();\n}\n\n// A sha256/ directory containing digest-named subdirectories\npublic interface DigestDir extends S3.Dir {\n    DigestEntry digest(String hash);\n    Stream&lt;DigestEntry&gt; digests();\n}\n\n// A single digest entry directory\npublic interface DigestEntry extends S3.Dir {\n    LinkFile link();\n}\n\n// A link file — contains a digest reference as text\npublic interface LinkFile extends S3.File {\n\n    // Parse the digest algorithm from the link content\n    default String getAlgorithm() {\n        String content = getValueAsString().trim();\n        return content.substring(0, content.indexOf(':'));\n    }\n\n    // Parse the digest hash from the link content\n    default String getDigest() {\n        String content = getValueAsString().trim();\n        return content.substring(content.indexOf(':') + 1);\n    }\n}\n\n// The blobs/ directory at the registry root\npublic interface BlobStore extends S3.Dir {\n    DigestDir sha256();\n}\n\n// A blob's data file — binary content\npublic interface BlobData extends S3.File {\n\n    default String getContentType() {\n        return getObjectMetadata().contentType();\n    }\n\n    default boolean isManifest() {\n        String ct = getContentType();\n        return ct != null &amp;&amp; ct.contains(\"manifest\");\n    }\n}\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#usage","level":2,"title":"Usage","text":"","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#resolve-an-image-tag-to-its-digest","level":3,"title":"Resolve an image tag to its digest","text":"<p>Navigate through the deep interface chain to read the digest a tag points to:</p> <pre><code>Registry registry = bucket.as(Registry.class);\n\nImageRepo apiServer = registry.repositories()\n    .namespace(\"myorg\")\n    .image(\"api-server\");\n\n// Follow the chain: _manifests → tags → v1.0.0 → current → link\nLinkFile link = apiServer.manifests()\n    .tags()\n    .tag(\"v1.0.0\")\n    .current()\n    .link();\n\nSystem.out.println(link.getAlgorithm());\n// Output: sha256\n\nSystem.out.println(link.getDigest());\n// Output: abc123...\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#list-all-tags-for-an-image","level":3,"title":"List all tags for an image","text":"<pre><code>apiServer.manifests().tags().tags().forEach(tag -&gt; {\n    System.out.println(tag.file().getName());\n});\n// Output:\n//   latest\n//   v1.0.0\n//   v1.1.0\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#access-binary-blob-data","level":3,"title":"Access binary blob data","text":"<p><code>getValueAsStream()</code> returns the raw bytes for binary objects like container layers:</p> <pre><code>BlobStore blobs = registry.blobs();\n\nDigestEntry entry = blobs.sha256().digest(\"def456...\");\nLinkFile blobLink = entry.link();\nSystem.out.println(blobLink.getValueAsString().trim());\n// Output: sha256:def456...\n\n// Read binary blob content as a stream\nBlobData blob = blobs.sha256().digest(\"abc123...\").data();\n\ntry (InputStream is = blob.getValueAsStream()) {\n    // Process the binary content...\n}\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#inspect-content-types","level":3,"title":"Inspect content types","text":"<p>Default methods on <code>BlobData</code> add domain logic for identifying blob types:</p> <pre><code>BlobData blob = registry.blobs().sha256().digest(\"abc123...\").data();\n\nSystem.out.println(blob.getContentType());\n// Output: application/vnd.oci.image.manifest.v1+json\n\nSystem.out.println(blob.isManifest());\n// Output: true\n</code></pre>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#features-used","level":2,"title":"Features Used","text":"<ul> <li><code>@Name</code> — maps methods to <code>_manifests</code>, <code>_layers</code>, and other keys that start with underscores</li> <li>Deep interface chaining — models the nested registry path: <code>repositories → namespace → image → _manifests → tags → tag → current → link</code></li> <li><code>getValueAsStream()</code> — reads binary blob data</li> <li>Default methods — <code>LinkFile.getAlgorithm()</code>, <code>LinkFile.getDigest()</code>, <code>BlobData.isManifest()</code> add parsing logic</li> <li><code>getObjectMetadata()</code> — <code>BlobData.getContentType()</code> reads the S3 content type</li> </ul>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/container-registry/#test","level":2,"title":"Test","text":"<p>See ContainerRegistryTest.java for a working test of this example.</p>","path":["Examples","Container Registry"],"tags":[]},{"location":"examples/data-lake/","level":1,"title":"Data Lake / ETL Pipeline","text":"<p>This example models a data lake with Hive-style partitioned directories stored in S3. It demonstrates <code>@Name</code> for keys containing <code>=</code> characters, <code>@Marker</code> for pagination control, <code>@Prefix</code> for partition searches, and collection return types like <code>List&lt;&gt;</code> and arrays.</p>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#s3-bucket-structure","level":2,"title":"S3 Bucket Structure","text":"<pre><code>data-lake/\n  raw/\n    events-2025-01-15.json\n    events-2025-01-16.json\n    events-2025-02-01.json\n  processed/\n    year=2025/\n      month=01/\n        day=15/\n          part-00000.parquet\n          part-00001.parquet\n          _SUCCESS\n        day=16/\n          part-00000.parquet\n          _SUCCESS\n      month=02/\n        day=01/\n          part-00000.parquet\n          _SUCCESS\n  curated/\n    daily-summary-2025-01-15.csv\n    daily-summary-2025-01-16.csv\n    daily-summary-2025-02-01.csv\n    monthly-summary-2025-01.csv\n    monthly-summary-2025-02.csv\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#interfaces","level":2,"title":"Interfaces","text":"<pre><code>// Root of the data lake\npublic interface DataLake extends S3.Dir {\n    Raw raw();\n    Processed processed();\n    Curated curated();\n}\n\n// Raw ingestion zone — unpartitioned event files\npublic interface Raw extends S3.Dir {\n    Stream&lt;EventFile&gt; events();\n\n    // Start listing after the last January event\n    @Marker(\"events-2025-01-16.json\")\n    Stream&lt;EventFile&gt; eventsSinceFeb();\n\n    // Only events matching a prefix\n    @Prefix(\"events-2025-01\")\n    Stream&lt;EventFile&gt; januaryEvents();\n}\n\n// Processed zone — Hive-style year=YYYY/month=MM/day=DD partitions\npublic interface Processed extends S3.Dir {\n\n    // @Name maps to \"year=2025\" — the = character can't be a method name\n    @Name(\"year=2025\")\n    YearPartition year2025();\n\n    // Recursively list all parquet files across all partitions\n    @Recursive\n    @Filter(IsParquet.class)\n    Stream&lt;S3File&gt; allParquetFiles();\n\n    // All year partitions as a list\n    List&lt;YearPartition&gt; years();\n}\n\n// year=YYYY partition\npublic interface YearPartition extends S3.Dir {\n\n    // Named lookup: pass \"month=01\" to get that partition\n    MonthPartition partition(String name);\n\n    @Name(\"month=01\")\n    MonthPartition month01();\n\n    @Name(\"month=02\")\n    MonthPartition month02();\n\n    // All month partitions as an array\n    MonthPartition[] months();\n}\n\n// month=MM partition\npublic interface MonthPartition extends S3.Dir {\n\n    DayPartition partition(String name);\n\n    // All day partitions as a list\n    List&lt;DayPartition&gt; days();\n}\n\n// day=DD partition — contains the actual data files\npublic interface DayPartition extends S3.Dir {\n\n    // All data files in this partition (parquet + _SUCCESS)\n    Stream&lt;DataFile&gt; dataFiles();\n\n    // Only parquet files\n    @Filter(IsParquet.class)\n    Stream&lt;DataFile&gt; parquetFiles();\n}\n\n// A data file (parquet, _SUCCESS marker, etc.)\npublic interface DataFile extends S3.File {\n}\n\n// A raw event file\npublic interface EventFile extends S3.File {\n}\n\n// Curated zone — summary reports\npublic interface Curated extends S3.Dir {\n    Stream&lt;ReportFile&gt; reports();\n\n    @Prefix(\"daily-summary\")\n    Stream&lt;ReportFile&gt; dailySummaries();\n\n    @Prefix(\"monthly-summary\")\n    Stream&lt;ReportFile&gt; monthlySummaries();\n\n    // Start listing after a known key\n    @Marker(\"daily-summary-2025-01-15.csv\")\n    Stream&lt;ReportFile&gt; reportsAfterJan15();\n}\n\n// A curated report file\npublic interface ReportFile extends S3.File {\n}\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#filter-predicate","level":3,"title":"Filter predicate","text":"<pre><code>public class IsParquet implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".parquet\");\n    }\n}\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#usage","level":2,"title":"Usage","text":"","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#navigate-hive-style-partitions","level":3,"title":"Navigate Hive-style partitions","text":"<p>The <code>@Name</code> annotation maps methods to key segments containing <code>=</code>:</p> <pre><code>DataLake lake = bucket.as(DataLake.class);\n\nYearPartition year = lake.processed().year2025();\nMonthPartition jan = year.month01();\n\njan.days().forEach(day -&gt; {\n    System.out.println(day.file().getName());\n});\n// Output:\n//   day=15\n//   day=16\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#recursively-list-all-parquet-files","level":3,"title":"Recursively list all parquet files","text":"<p><code>@Recursive</code> combined with <code>@Filter</code> finds every parquet file across all partitions:</p> <pre><code>lake.processed().allParquetFiles().forEach(f -&gt; {\n    System.out.println(f.getAbsoluteName());\n});\n// Output:\n//   data-lake/processed/year=2025/month=01/day=15/part-00000.parquet\n//   data-lake/processed/year=2025/month=01/day=15/part-00001.parquet\n//   data-lake/processed/year=2025/month=01/day=16/part-00000.parquet\n//   data-lake/processed/year=2025/month=02/day=01/part-00000.parquet\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#prefix-search-for-summaries","level":3,"title":"Prefix search for summaries","text":"<p><code>@Prefix</code> narrows listings server-side:</p> <pre><code>lake.curated().dailySummaries().forEach(r -&gt; {\n    System.out.println(r.file().getName());\n});\n// Output:\n//   daily-summary-2025-01-15.csv\n//   daily-summary-2025-01-16.csv\n//   daily-summary-2025-02-01.csv\n\nlake.curated().monthlySummaries().forEach(r -&gt; {\n    System.out.println(r.file().getName());\n});\n// Output:\n//   monthly-summary-2025-01.csv\n//   monthly-summary-2025-02.csv\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#pagination-with-marker","level":3,"title":"Pagination with @Marker","text":"<p><code>@Marker</code> starts the listing after the given key, useful for resuming interrupted scans over large datasets:</p> <pre><code>// Skip everything up to and including daily-summary-2025-01-15.csv\nlake.curated().reportsAfterJan15().forEach(r -&gt; {\n    System.out.println(r.file().getName());\n});\n// Output:\n//   daily-summary-2025-01-16.csv\n//   daily-summary-2025-02-01.csv\n//   monthly-summary-2025-01.csv\n//   monthly-summary-2025-02.csv\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#list-and-array-return-types","level":3,"title":"List and array return types","text":"<p>Methods can return <code>List&lt;&gt;</code> or arrays instead of <code>Stream&lt;&gt;</code>:</p> <pre><code>// List&lt;YearPartition&gt; — all year partitions collected into a List\nList&lt;YearPartition&gt; years = lake.processed().years();\nSystem.out.println(years.size());\n// Output: 1\n\n// MonthPartition[] — all month partitions as an array\nMonthPartition[] months = lake.processed().year2025().months();\nSystem.out.println(months.length);\n// Output: 2\n</code></pre>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#features-used","level":2,"title":"Features Used","text":"<ul> <li><code>@Name</code> — maps methods to Hive-style keys like <code>year=2025</code> and <code>month=01</code></li> <li><code>@Prefix</code> — server-side filtering for <code>dailySummaries()</code> and <code>monthlySummaries()</code></li> <li><code>@Marker</code> — resume listing from a known position with <code>reportsAfterJan15()</code></li> <li><code>@Filter</code> (method-level) — <code>IsParquet</code> predicate on <code>allParquetFiles()</code> and <code>parquetFiles()</code></li> <li><code>@Recursive</code> — recursive listing across all Hive partitions</li> <li><code>List&lt;&gt;</code> and array returns — <code>years()</code> returns <code>List&lt;YearPartition&gt;</code>, <code>months()</code> returns <code>MonthPartition[]</code></li> </ul>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/data-lake/#test","level":2,"title":"Test","text":"<p>See DataLakeTest.java for a working test of this example.</p>","path":["Examples","Data Lake / ETL Pipeline"],"tags":[]},{"location":"examples/log-archives/","level":1,"title":"Log Archives by Date","text":"<p>This example models date-partitioned application logs stored in S3. It demonstrates method-level <code>@Filter</code> for selecting log types and <code>@Parent</code> for navigating back up the hierarchy.</p>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#s3-bucket-structure","level":2,"title":"S3 Bucket Structure","text":"<pre><code>logs/\n  production/\n    2024/\n      12/\n        30/\n          app-2024-12-30.log.gz\n          access-2024-12-30.log.gz\n        31/\n          app-2024-12-31.log.gz\n          access-2024-12-31.log.gz\n    2025/\n      01/\n        01/\n          app-2025-01-01.log.gz\n          access-2025-01-01.log.gz\n        02/\n          app-2025-01-02.log.gz\n          access-2025-01-02.log.gz\n      02/\n        01/\n          app-2025-02-01.log.gz\n          access-2025-02-01.log.gz\n  staging/\n    2025/\n      01/\n        01/\n          app-2025-01-01.log.gz\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#interfaces","level":2,"title":"Interfaces","text":"<pre><code>// Root of the logs bucket\npublic interface LogBucket extends S3.Dir {\n    Stream&lt;Environment&gt; environments();\n    Environment environment(String name);\n}\n\n// An environment (production, staging)\npublic interface Environment extends S3.Dir {\n    Stream&lt;YearDir&gt; years();\n    YearDir year(String year);\n\n    // All year directories (immediate listing — S3.Dir type)\n    Stream&lt;YearDir&gt; allYears();\n}\n\n// Year directory (2024, 2025, ...)\npublic interface YearDir extends S3.Dir {\n    Stream&lt;MonthDir&gt; months();\n    MonthDir month(String month);\n}\n\n// Month directory (01, 02, ..., 12)\npublic interface MonthDir extends S3.Dir {\n    Stream&lt;DayDir&gt; days();\n    DayDir day(String day);\n}\n\n// Day directory (01, 02, ..., 31)\npublic interface DayDir extends S3.Dir {\n    Stream&lt;LogFile&gt; logs();\n\n    @Filter(IsAppLog.class)\n    Stream&lt;LogFile&gt; appLogs();\n\n    @Filter(IsAccessLog.class)\n    Stream&lt;LogFile&gt; accessLogs();\n\n    // Navigate back to the environment (day → month → year → env)\n    @Parent(3)\n    Environment environment();\n}\n\n// A single log file\npublic interface LogFile extends S3.File {\n}\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#filter-predicates","level":3,"title":"Filter predicates","text":"<pre><code>public class IsAppLog implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().startsWith(\"app-\");\n    }\n}\n\npublic class IsAccessLog implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().startsWith(\"access-\");\n    }\n}\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#usage","level":2,"title":"Usage","text":"","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#navigate-the-date-hierarchy","level":3,"title":"Navigate the date hierarchy","text":"<p>Access logs by walking the year → month → day structure:</p> <pre><code>LogBucket root = bucket.as(LogBucket.class);\nEnvironment prod = root.environment(\"production\");\n\nDayDir day = prod.year(\"2025\").month(\"01\").day(\"01\");\nday.logs().forEach(log -&gt; {\n    System.out.println(log.file().getName());\n});\n// Output:\n//   app-2025-01-01.log.gz\n//   access-2025-01-01.log.gz\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#filter-by-log-type","level":3,"title":"Filter by log type","text":"<p>Method-level <code>@Filter</code> on <code>DayDir</code> separates application logs from access logs:</p> <pre><code>DayDir day = prod.year(\"2025\").month(\"01\").day(\"01\");\n\nday.appLogs().forEach(log -&gt; {\n    System.out.println(log.file().getName());\n});\n// Output:\n//   app-2025-01-01.log.gz\n\nday.accessLogs().forEach(log -&gt; {\n    System.out.println(log.file().getName());\n});\n// Output:\n//   access-2025-01-01.log.gz\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#list-year-directories","level":3,"title":"List year directories","text":"<p>Since <code>YearDir</code> extends <code>S3.Dir</code>, the listing returns only immediate directories — no <code>@Recursive</code> annotation needed:</p> <pre><code>prod.allYears().forEach(year -&gt; {\n    System.out.println(year.file().getName());\n});\n// Output:\n//   2024\n//   2025\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#navigate-back-with-parent","level":3,"title":"Navigate back with @Parent","text":"<p>From any day directory, <code>@Parent(3)</code> jumps back to the environment:</p> <pre><code>DayDir day = prod.year(\"2025\").month(\"01\").day(\"01\");\n\n// @Parent(3): day → month → year → environment\nEnvironment env = day.environment();\nSystem.out.println(env.file().getName());\n// Output: production\n</code></pre>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#features-used","level":2,"title":"Features Used","text":"<ul> <li>Immediate listing — <code>allYears()</code> returns immediate year directories</li> <li><code>@Filter</code> (method-level) — <code>appLogs()</code> and <code>accessLogs()</code> on <code>DayDir</code> filter by log type</li> <li><code>@Parent</code> — <code>@Parent(3)</code> navigates from day back to environment</li> </ul>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/log-archives/#test","level":2,"title":"Test","text":"<p>See LogArchivesTest.java for a working test of this example.</p>","path":["Examples","Log Archives by Date"],"tags":[]},{"location":"examples/maven-project/","level":1,"title":"Maven Multi-Module Project","text":"<p>This example models a Maven multi-module project source tree stored in S3. It demonstrates navigating between modules, accessing named files like <code>pom.xml</code>, and using <code>@Parent</code> to walk back up the directory hierarchy.</p>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#s3-bucket-structure","level":2,"title":"S3 Bucket Structure","text":"<pre><code>my-project/\n  pom.xml\n  README.md\n  core/\n    pom.xml\n    src/\n      main/\n        java/\n          App.java\n          Config.java\n        resources/\n          application.properties\n      test/\n        java/\n          AppTest.java\n        resources/\n          test.properties\n  web/\n    pom.xml\n    src/\n      main/\n        java/\n          WebApp.java\n          Controller.java\n        resources/\n          templates/\n            index.html\n      test/\n        java/\n          WebAppTest.java\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#interfaces","level":2,"title":"Interfaces","text":"<pre><code>// Root project — contains the parent pom and modules\npublic interface Project extends S3.Dir {\n\n    @Name(\"pom.xml\")\n    PomFile pomXml();\n\n    @Name(\"README.md\")\n    ReadmeFile readme();\n\n    // List all modules as directories\n    Stream&lt;Module&gt; modules();\n\n    // Jump directly to a module by name\n    Module module(String name);\n}\n\n// A single module (core, web, etc.)\npublic interface Module extends S3.Dir {\n\n    @Name(\"pom.xml\")\n    PomFile pomXml();\n\n    Src src();\n\n    // Navigate back to the project root\n    @Parent\n    Project project();\n}\n\n// The src/ directory splits into main and test\npublic interface Src extends S3.Dir {\n    Section main();\n    Section test();\n}\n\n// A section (main or test) contains java and resources\npublic interface Section extends S3.Dir {\n    SourceDir java();\n    SourceDir resources();\n\n    // Navigate back to the module (src → module)\n    @Parent(2)\n    Module module();\n}\n\n// A directory of source files\npublic interface SourceDir extends S3.Dir {\n    Stream&lt;SourceFile&gt; sources();\n}\n\n// An individual source file\npublic interface SourceFile extends S3.File {\n}\n\n// pom.xml — readable as a string\npublic interface PomFile extends S3.File {\n}\n\n// README.md — readable as a string\npublic interface ReadmeFile extends S3.File {\n}\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#usage","level":2,"title":"Usage","text":"","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#list-all-modules","level":3,"title":"List all modules","text":"<pre><code>Project project = bucket.as(Project.class);\n\nproject.modules().forEach(module -&gt; {\n    System.out.println(module.file().getName());\n});\n// Output:\n//   core\n//   web\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#read-a-modules-pomxml","level":3,"title":"Read a module's pom.xml","text":"<pre><code>Module core = project.module(\"core\");\n\nString pom = core.pomXml().getValueAsString();\nSystem.out.println(pom);\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#navigate-from-sources-back-to-the-project","level":3,"title":"Navigate from sources back to the project","text":"<p>The <code>@Parent</code> annotation lets you walk back up the tree without tracking references manually:</p> <pre><code>Module web = project.module(\"web\");\nSection main = web.src().main();\n\n// @Parent(2) jumps from section → src → module\nModule parent = main.module();\nSystem.out.println(parent.file().getName());\n// Output: web\n\n// From the module, @Parent jumps to the project\nProject root = parent.project();\nSystem.out.println(root.readme().getValueAsString());\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#list-source-files-in-a-module","level":3,"title":"List source files in a module","text":"<pre><code>Module core = project.module(\"core\");\n\ncore.src().main().java().sources().forEach(f -&gt; {\n    System.out.println(f.file().getAbsoluteName());\n});\n// Output:\n//   my-project/core/src/main/java/App.java\n//   my-project/core/src/main/java/Config.java\n</code></pre>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#features-used","level":2,"title":"Features Used","text":"<ul> <li><code>@Name</code> — maps <code>pomXml()</code> to <code>pom.xml</code> and <code>readme()</code> to <code>README.md</code></li> <li><code>@Parent</code> — navigates from module back to project, and from section back to module with <code>@Parent(2)</code></li> <li>Named child lookup — <code>module(String name)</code> for direct access to a module by name</li> <li><code>Stream&lt;Dir&gt;</code> — <code>modules()</code> returns only directory entries</li> <li><code>S3.File</code> — <code>PomFile</code> and <code>ReadmeFile</code> extend <code>S3.File</code> for content access</li> </ul>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/maven-project/#test","level":2,"title":"Test","text":"<p>See MavenProjectTest.java for a working test of this example.</p>","path":["Examples","Maven Multi-Module Project"],"tags":[]},{"location":"examples/static-website/","level":1,"title":"Static Website Assets","text":"<p>This example models versioned static website deployments stored in S3. It demonstrates type-level <code>@Filter</code> to categorize assets, default methods for domain logic, and <code>getObjectMetadata()</code> for inspecting content types.</p>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#s3-bucket-structure","level":2,"title":"S3 Bucket Structure","text":"<pre><code>deployments/\n  20250301-143022/\n    index.html\n    manifest.json\n    css/\n      main.css\n      reset.css\n    js/\n      app.js\n      vendor.js\n    images/\n      logo.png\n      hero.jpg\n  20250215-091500/\n    index.html\n    manifest.json\n    css/\n      main.css\n    js/\n      app.js\n    images/\n      logo.png\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#interfaces","level":2,"title":"Interfaces","text":"<pre><code>// Root of the deployments bucket\npublic interface Deployments extends S3.Dir {\n    Stream&lt;Deployment&gt; deployments();\n    Deployment deployment(String timestamp);\n}\n\n// A single timestamped deployment\npublic interface Deployment extends S3.Dir {\n\n    @Name(\"index.html\")\n    HtmlFile indexHtml();\n\n    @Name(\"manifest.json\")\n    Manifest manifest();\n\n    // Navigate into asset subdirectories\n    CssDir css();\n    JsDir js();\n    ImagesDir images();\n\n    // Top-level files only (index.html, manifest.json)\n    Stream&lt;Asset&gt; assets();\n\n    // Recursively list the full directory tree\n    @Recursive\n    Stream&lt;S3.Dir&gt; layout();\n\n    // Parse the deployment timestamp from the directory name\n    default LocalDateTime getDeployedAt() {\n        DateTimeFormatter fmt = DateTimeFormatter.ofPattern(\"yyyyMMdd-HHmmss\");\n        return LocalDateTime.parse(file().getName(), fmt);\n    }\n}\n\n// The css/ directory\npublic interface CssDir extends S3.Dir {\n    Stream&lt;CssFile&gt; stylesheets();\n}\n\n// The js/ directory\npublic interface JsDir extends S3.Dir {\n    Stream&lt;JsFile&gt; scripts();\n}\n\n// The images/ directory\npublic interface ImagesDir extends S3.Dir {\n    Stream&lt;ImageFile&gt; images();\n}\n\n// manifest.json with a convenience method\npublic interface Manifest extends S3.File {\n\n    default String getVersion() {\n        // Parse the version from the JSON content\n        String json = getValueAsString();\n        int start = json.indexOf(\"\\\"version\\\"\");\n        int colon = json.indexOf(':', start);\n        int open = json.indexOf('\"', colon + 1);\n        int close = json.indexOf('\"', open + 1);\n        return json.substring(open + 1, close);\n    }\n}\n\n// Base type for all static assets\npublic interface Asset extends S3.File {\n\n    default String getContentType() {\n        return getObjectMetadata().contentType();\n    }\n}\n\n// CSS files — type-level filter ensures only .css files are returned\n@Filter(IsCss.class)\npublic interface CssFile extends S3.File {\n}\n\n// JS files — type-level filter ensures only .js files are returned\n@Filter(IsJs.class)\npublic interface JsFile extends S3.File {\n}\n\n// Image files\n@Filter(IsImage.class)\npublic interface ImageFile extends S3.File {\n}\n\n// HTML files\npublic interface HtmlFile extends S3.File {\n}\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#filter-predicates","level":3,"title":"Filter predicates","text":"<pre><code>public class IsCss implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".css\");\n    }\n}\n\npublic class IsJs implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".js\");\n    }\n}\n\npublic class IsImage implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        String name = file.getName();\n        return name.endsWith(\".png\")\n            || name.endsWith(\".jpg\")\n            || name.endsWith(\".gif\")\n            || name.endsWith(\".svg\");\n    }\n}\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#usage","level":2,"title":"Usage","text":"","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#list-all-deployments","level":3,"title":"List all deployments","text":"<p>The <code>getDeployedAt()</code> default method uses <code>file().getName()</code> to read the directory name and parse it into a <code>LocalDateTime</code>:</p> <pre><code>Deployments root = bucket.as(Deployments.class);\n\nroot.deployments().forEach(d -&gt; {\n    System.out.println(d.file().getName() + \" → \" + d.getDeployedAt());\n});\n// Output:\n//   20250215-091500 → 2025-02-15T09:15\n//   20250301-143022 → 2025-03-01T14:30:22\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#read-the-manifest-and-check-the-version","level":3,"title":"Read the manifest and check the version","text":"<pre><code>Deployment latest = root.deployment(\"20250301-143022\");\n\nManifest manifest = latest.manifest();\nSystem.out.println(manifest.getVersion());\n// Output: 1.4.2\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#filter-assets-by-type","level":3,"title":"Filter assets by type","text":"<p>Each asset subdirectory is modeled as an <code>S3.Dir</code>, so the listing is scoped to the correct prefix. Type-level <code>@Filter</code> annotations on <code>CssFile</code>, <code>JsFile</code>, and <code>ImageFile</code> add an extra layer of safety:</p> <pre><code>Deployment deploy = root.deployment(\"20250301-143022\");\n\ndeploy.css().stylesheets().forEach(css -&gt; {\n    System.out.println(css.file().getName() + \" — \" + css.getSize() + \" bytes\");\n});\n// Output:\n//   main.css — 14230 bytes\n//   reset.css — 1082 bytes\n\ndeploy.js().scripts().forEach(js -&gt; {\n    System.out.println(js.file().getName());\n});\n// Output:\n//   app.js\n//   vendor.js\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#inspect-top-level-assets","level":3,"title":"Inspect top-level assets","text":"<p>Without <code>@Recursive</code>, <code>Stream&lt;Asset&gt;</code> performs a single delimiter-based listing and returns only the files at the deployment level:</p> <pre><code>Deployment deploy = root.deployment(\"20250301-143022\");\n\ndeploy.assets().forEach(asset -&gt; {\n    System.out.println(asset.file().getName() + \" → \" + asset.getContentType());\n});\n// Output:\n//   index.html → text/html\n//   manifest.json → application/json\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#view-the-deployment-layout","level":3,"title":"View the deployment layout","text":"<p><code>@Recursive</code> recursively descends into every subdirectory. Because the element type is <code>S3.Dir</code>, only directories are returned — files are filtered out:</p> <pre><code>Deployment deploy = root.deployment(\"20250301-143022\");\n\ndeploy.layout().forEach(entry -&gt; {\n    System.out.println(entry.file().getAbsoluteName());\n});\n// Output:\n//   deployments/20250301-143022/css\n//   deployments/20250301-143022/images\n//   deployments/20250301-143022/js\n</code></pre>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#features-used","level":2,"title":"Features Used","text":"<ul> <li><code>S3.Dir</code> — <code>CssDir</code>, <code>JsDir</code>, and <code>ImagesDir</code> scope listings to the correct subdirectory</li> <li><code>@Filter</code> (type-level) — <code>CssFile</code>, <code>JsFile</code>, and <code>ImageFile</code> interfaces carry their own filter predicates</li> <li><code>@Recursive</code> — <code>layout()</code> recursively lists the deployment tree</li> <li><code>S3.File</code> content access — <code>getValueAsString()</code> on <code>Manifest</code>, <code>getSize()</code> on assets</li> <li>Default methods — <code>Deployment.getDeployedAt()</code> parses the directory name via <code>file().getName()</code>, <code>Manifest.getVersion()</code> and <code>Asset.getContentType()</code> add domain logic</li> <li><code>getObjectMetadata()</code> — <code>Asset.getContentType()</code> reads the S3 content type header</li> <li><code>@Name</code> — maps methods to <code>index.html</code> and <code>manifest.json</code></li> </ul>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"examples/static-website/#test","level":2,"title":"Test","text":"<p>See StaticWebsiteTest.java for a working test of this example.</p>","path":["Examples","Static Website Assets"],"tags":[]},{"location":"guide/annotations/","level":1,"title":"Annotations","text":"<p>JAWS provides annotations to control how proxy methods map to S3 operations. They can customize key resolution, traversal behavior, and result filtering.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#name","level":2,"title":"@Name","text":"<p>Overrides the S3 key segment used when resolving a method to a child object. By default the method name is used; <code>@Name</code> allows mapping to keys that can't be expressed as Java identifiers.</p> <pre><code>public interface Module extends S3.Dir {\n    @Name(\"pom.xml\")\n    PomFile pomXml();\n\n    @Name(\"src/main/java\")\n    Sources sources();\n}\n</code></pre> <p>Without <code>@Name</code>, <code>pomXml()</code> would resolve to the key segment <code>pomXml</code>. With <code>@Name(\"pom.xml\")</code>, it resolves to <code>pom.xml</code>.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#parent","level":2,"title":"@Parent","text":"<p>Navigates upward in the S3 key hierarchy. The annotated method must return an interface extending <code>S3</code>. The value specifies how many levels to go up (default is 1).</p> <pre><code>public interface Version extends S3.Dir {\n    // Go up one level: version → artifact\n    @Parent\n    ArtifactDir artifact();\n\n    // Go up two levels: version → artifact → group\n    @Parent(2)\n    GroupDir group();\n\n    // Go up three levels: version → artifact → group → repository root\n    @Parent(3)\n    Repository repository();\n}\n</code></pre> <p>This is useful for navigating from a deeply nested proxy back to an ancestor without manually tracking the path.</p> <p>Warning</p> <p>A <code>NoParentException</code> is thrown at runtime if the proxy is already at the bucket root and no further parent exists.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#recursive","level":2,"title":"@Recursive","text":"<p>Marks a listing method as recursive — all descendants are returned, not just immediate children. Without <code>@Recursive</code>, all listing methods use a delimiter-based listing that returns only immediate children at one level.</p> <pre><code>public interface Repository extends S3.Dir {\n    // All descendant objects (flat listing, single request)\n    @Recursive\n    Stream&lt;S3File&gt; everything();\n\n    // All descendant directories (tree walk, one request per prefix)\n    @Recursive\n    Stream&lt;S3.Dir&gt; layout();\n}\n</code></pre> <p><code>@Recursive</code> is a marker annotation with no attributes. There is no depth control; for depth-limited traversal, use the <code>S3File.walk()</code> methods directly.</p> <p>See Listing &amp; Recursion for a detailed walkthrough.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#prefix","level":2,"title":"@Prefix","text":"<p>Narrows a listing to only S3 keys that begin with the given prefix. The value is relative — it is appended to the current proxy's position in the key hierarchy. The prefix is sent server-side in the <code>ListObjects</code> request, so non-matching keys are never returned by AWS.</p> <pre><code>public interface Repository extends S3.Dir {\n    // Only list artifacts under org/apache\n    @Prefix(\"org/apache\")\n    Stream&lt;S3File&gt; apacheArtifacts();\n\n    // Recursively list only keys under the org/ namespace\n    @Recursive\n    @Prefix(\"org/\")\n    Stream&lt;S3File&gt; orgArtifacts();\n}\n</code></pre>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#partial-prefixes","level":3,"title":"Partial prefixes","text":"<p>The prefix doesn't need to end with a delimiter. It can be any key prefix, including a partial name:</p> <pre><code>public interface Tomcat extends S3.Dir {\n    // Only version directories starting with \"10.\"\n    // Matches 10.1.18/, 10.1.19/, etc.\n    // Skips 9.0.85/, 11.0.0/, etc.\n    @Prefix(\"10.\")\n    Stream&lt;S3File&gt; version10();\n}\n</code></pre> <p>Because the prefix is applied server-side, a directory with hundreds of entries can be efficiently narrowed to just the relevant subset.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#suffix","level":2,"title":"@Suffix","text":"<p>Filters results by file name ending. A shorthand for the common case of filtering by file extension. Multiple values are OR'd.</p> <pre><code>public interface Assets extends S3.Dir {\n    @Suffix(\".css\")\n    Stream&lt;S3File&gt; stylesheets();\n\n    @Suffix({\".jpg\", \".png\", \".gif\"})\n    Stream&lt;S3File&gt; images();\n}\n</code></pre> <p><code>@Suffix</code> is repeatable with an <code>exclude</code> flag. Use <code>exclude = true</code> to remove entries matching a suffix:</p> <pre><code>public interface Assets extends S3.Dir {\n    // All .jar files except sources and javadoc jars\n    @Suffix(\".jar\")\n    @Suffix(value = {\"-sources.jar\", \"-javadoc.jar\"}, exclude = true)\n    Stream&lt;S3File&gt; binaryJars();\n}\n</code></pre>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#type-level-suffix","level":3,"title":"Type-level suffix","text":"<p><code>@Suffix</code> can also be placed on an interface. It applies to all methods that return that type — both listings and single-arg lookups:</p> <pre><code>@Suffix(\".parquet\")\npublic interface ParquetFile extends S3.File {\n    // Stream&lt;ParquetFile&gt; listings filter to .parquet files only\n    // ParquetFile partition(String name) validates name ends with .parquet\n}\n</code></pre>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#match","level":2,"title":"@Match","text":"<p>Filters results by a regular expression on the file name. The entire name must match (implied <code>^</code> and <code>$</code>). Uses <code>Pattern.asMatchPredicate()</code>.</p> <pre><code>public interface Reports extends S3.Dir {\n    @Match(\"daily-\\\\d{4}-\\\\d{2}-\\\\d{2}\\\\.csv\")\n    Stream&lt;S3File&gt; dailyReports();\n\n    @Match(\".*\\\\.(jpg|png)\")\n    Stream&lt;S3File&gt; images();\n}\n</code></pre> <p><code>@Match</code> is repeatable with an <code>exclude</code> flag. Use <code>exclude = true</code> to remove entries matching a pattern:</p> <pre><code>public interface Assets extends S3.Dir {\n    // All CSS files except reset.css\n    @Match(\".*\\\\.css\")\n    @Match(value = \"reset\\\\.css\", exclude = true)\n    Stream&lt;S3File&gt; cssExceptReset();\n}\n</code></pre> <p><code>@Match</code> can also be placed on an interface for type-level filtering and validation. Listings return only matching entries, and single-arg methods reject names that don't match.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#filter","level":2,"title":"@Filter","text":"<p>Applies an arbitrary client-side predicate to filter results. The annotation takes a <code>Predicate&lt;S3File&gt;</code> class that must have a no-arg constructor.</p> <pre><code>public class IsJar implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".jar\");\n    }\n}\n\npublic interface VersionDir extends S3.Dir {\n    @Filter(IsJar.class)\n    Stream&lt;S3File&gt; jars();\n}\n</code></pre>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#type-level-filters","level":3,"title":"Type-level filters","text":"<p><code>@Filter</code> can also be placed on an interface. It applies to all methods that return that type — both listings and single-arg lookups:</p> <pre><code>@Filter(IsJar.class)\npublic interface JarFile extends S3.File {\n    // Stream&lt;JarFile&gt; listings filter to .jar files only\n    // JarFile artifact(String name) validates name passes IsJar\n}\n\npublic interface VersionDir extends S3.Dir {\n    Stream&lt;JarFile&gt; artifacts();  // only .jar files\n}\n</code></pre> <p><code>@Filter</code> is repeatable — multiple filters are combined with AND logic.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#filter-evaluation-order","level":3,"title":"Filter evaluation order","text":"<p>When multiple filter annotations are present, they are applied in order — simplest first, most complex last:</p> <ol> <li>@Prefix — server-side</li> <li>@Suffix includes — <code>String.endsWith()</code></li> <li>@Suffix excludes</li> <li>@Match includes — compiled regex</li> <li>@Match excludes</li> <li>@Filter — arbitrary <code>Predicate&lt;S3File&gt;</code></li> </ol> <p>Each filter only sees entries that passed the previous ones. Interface-level filters run before method-level filters within each category.</p> <p>Tip</p> <p>Use <code>@Suffix</code> for extension checks and <code>@Match</code> for name patterns. Reserve <code>@Filter</code> for cases that need access to the full <code>S3File</code>. Prefer <code>@Prefix</code> over all of these when possible — it filters server-side and reduces the data transferred from AWS.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#delimiter","level":2,"title":"@Delimiter","text":"<p>Sets the delimiter for the <code>ListObjects</code> request. Can be used on both immediate listing and <code>@Recursive</code> methods.</p> <pre><code>public interface Logs extends S3.Dir {\n    @Delimiter(\"-\")\n    Stream&lt;S3File&gt; byDate();\n}\n</code></pre>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/annotations/#marker","level":2,"title":"@Marker","text":"<p>Sets the starting position for a listing. Keys before the marker are skipped. This is the low-level pagination mechanism used by S3.</p> <pre><code>public interface Archive extends S3.Dir {\n    @Marker(\"2024-01-01\")\n    Stream&lt;S3File&gt; since2024();\n}\n</code></pre> <p>Note</p> <p>In most cases, JAWS handles pagination automatically. <code>@Marker</code> is useful when you need to start a listing from a known position.</p>","path":["Guide","Annotations"],"tags":[]},{"location":"guide/default-methods/","level":1,"title":"Default Methods","text":"<p>Java default methods turn JAWS proxy interfaces from passive data mappings into rich domain objects. Because JAWS invokes default methods normally, you can layer business logic — serialization, validation, transformation — directly onto the interface that models your S3 structure.</p>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#basic-example","level":2,"title":"Basic Example","text":"<p>The simplest use is adding a convenience method to a file interface:</p> <pre><code>public interface Config extends S3.File {\n    default Properties asProperties() {\n        Properties p = new Properties();\n        p.load(new StringReader(getValueAsString()));\n        return p;\n    }\n}\n</code></pre> <p>Callers get a <code>Properties</code> object without knowing anything about S3:</p> <pre><code>Config config = bucket.getFile(\"app.properties\").as(Config.class);\nProperties props = config.asProperties();\n</code></pre>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#building-reusable-base-interfaces","level":2,"title":"Building Reusable Base Interfaces","text":"<p>Default methods become truly powerful when you build reusable base interfaces that sub-interfaces specialize with type information.</p>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#json-with-jakarta-json-b","level":3,"title":"JSON with Jakarta JSON-B","text":"<p>Consider a base interface that handles JSON marshaling:</p> <pre><code>public interface JsonbFile extends S3.File {\n\n    default &lt;T&gt; T fromJson(Class&lt;T&gt; type) {\n        try (Jsonb jsonb = JsonbBuilder.create()) {\n            return jsonb.fromJson(getValueAsString(), type);\n        } catch (Exception e) {\n            throw new IllegalStateException(e);\n        }\n    }\n\n    default &lt;T&gt; void toJson(T object) {\n        try (Jsonb jsonb = JsonbBuilder.create()) {\n            setValueAsString(jsonb.toJson(object));\n        } catch (Exception e) {\n            throw new IllegalStateException(e);\n        }\n    }\n}\n</code></pre> <p>Sub-interfaces supply the type and expose clean <code>read()</code> / <code>write()</code> methods:</p> <pre><code>public interface UserFile extends JsonbFile {\n    default User read() {\n        return fromJson(User.class);\n    }\n\n    default void write(User user) {\n        toJson(user);\n    }\n}\n\npublic interface OrderFile extends JsonbFile {\n    default Order read() {\n        return fromJson(Order.class);\n    }\n\n    default void write(Order order) {\n        toJson(order);\n    }\n}\n</code></pre> <p>Now a directory interface can expose fully typed accessors:</p> <pre><code>public interface UserDir extends S3.Dir {\n    Stream&lt;UserFile&gt; users();\n    UserFile user(String id);\n}\n</code></pre> <pre><code>UserDir dir = bucket.getFile(\"users\").as(UserDir.class);\n\n// Read\nUser alice = dir.user(\"alice.json\").read();\n\n// Write\ndir.user(\"bob.json\").write(new User(\"bob\", \"bob@example.com\"));\n\n// Stream all users\ndir.users()\n    .map(UserFile::read)\n    .filter(u -&gt; u.isActive())\n    .forEach(System.out::println);\n</code></pre> <p>The JSON-B boilerplate lives in one place. Every JSON-backed file in your bucket gets serialization for free by extending <code>JsonbFile</code>.</p>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#yaml-with-snakeyaml","level":3,"title":"YAML with SnakeYAML","text":"<p>The same pattern works for any format:</p> <pre><code>public interface YamlFile extends S3.File {\n\n    default &lt;T&gt; T fromYaml(Class&lt;T&gt; type) {\n        Yaml yaml = new Yaml(new Constructor(type, new LoaderOptions()));\n        return yaml.load(getValueAsString());\n    }\n\n    default &lt;T&gt; void toYaml(T object) {\n        Yaml yaml = new Yaml();\n        setValueAsString(yaml.dump(object));\n    }\n}\n\npublic interface PipelineFile extends YamlFile {\n    default Pipeline read() {\n        return fromYaml(Pipeline.class);\n    }\n\n    default void write(Pipeline pipeline) {\n        toYaml(pipeline);\n    }\n}\n</code></pre>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#csv","level":3,"title":"CSV","text":"<pre><code>public interface CsvFile extends S3.File {\n\n    default List&lt;String[]&gt; readCsv() {\n        try (CSVReader reader = new CSVReader(\n                new StringReader(getValueAsString()))) {\n            return reader.readAll();\n        } catch (Exception e) {\n            throw new IllegalStateException(e);\n        }\n    }\n\n    default List&lt;String[]&gt; readCsvSkipHeader() {\n        List&lt;String[]&gt; rows = readCsv();\n        return rows.subList(1, rows.size());\n    }\n}\n</code></pre>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#computed-properties","level":2,"title":"Computed Properties","text":"<p>Default methods work well for deriving information from file names or content without extra S3 calls:</p> <pre><code>public interface Artifact extends S3.File {\n\n    default String artifactId() {\n        // maven-core-3.9.6.jar → maven-core\n        String name = file().getName();\n        return name.substring(0, name.lastIndexOf('-'));\n    }\n\n    default String version() {\n        // maven-core-3.9.6.jar → 3.9.6\n        String name = file().getName();\n        String noExt = name.substring(0, name.lastIndexOf('.'));\n        return noExt.substring(noExt.lastIndexOf('-') + 1);\n    }\n\n    default String extension() {\n        String name = file().getName();\n        return name.substring(name.lastIndexOf('.') + 1);\n    }\n}\n</code></pre>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#combining-with-annotations","level":2,"title":"Combining with Annotations","text":"<p>Default methods compose naturally with JAWS annotations. The annotations control which files are listed; the default methods control what you can do with them:</p> <pre><code>@Suffix(\".json\")\npublic interface ConfigFile extends JsonbFile {\n    default AppConfig read() {\n        return fromJson(AppConfig.class);\n    }\n\n    default void write(AppConfig config) {\n        toJson(config);\n    }\n}\n\npublic interface ConfigDir extends S3.Dir {\n    // @Suffix(\".json\") on ConfigFile filters the listing\n    // default read()/write() methods give typed access\n    Stream&lt;ConfigFile&gt; configs();\n\n    ConfigFile config(String name);\n}\n</code></pre>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/default-methods/#tips","level":2,"title":"Tips","text":"<ul> <li>Keep base interfaces generic. <code>JsonbFile</code>, <code>YamlFile</code>, and <code>CsvFile</code>   should not know about specific domain types — that is the job of   sub-interfaces.</li> <li>Use <code>file()</code> and <code>parent()</code> freely. Default methods have access to   all of <code>S3.File</code>'s inherited methods, so you can inspect names, sizes,   metadata, and navigate the key hierarchy.</li> <li>Combine multiple base interfaces. A file interface can extend both   <code>S3.File</code> and implement a custom mixin interface if your defaults don't   need <code>S3.File</code> methods.</li> </ul>","path":["Guide","Default Methods"],"tags":[]},{"location":"guide/directories-and-files/","level":1,"title":"Directories &amp; Files","text":"<p>JAWS distinguishes between S3 directories (prefixes) and S3 objects (files) through the <code>S3.Dir</code> and <code>S3.File</code> sub-interfaces. The interface your proxy extends tells JAWS what operations are available and how listings should behave.</p>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#s3dir-directories","level":2,"title":"S3.Dir — Directories","text":"<p>Interfaces that model a folder or container of S3 objects extend <code>S3.Dir</code>:</p> <pre><code>public interface Photos extends S3.Dir {\n    Stream&lt;Album&gt; albums();\n    Album album(String name);\n}\n</code></pre> <p><code>S3.Dir</code> provides these built-in methods:</p> Method Description <code>file(String name)</code> Returns the child <code>S3File</code> with the given name <code>files()</code> All objects under this prefix recursively (flat, no delimiter) <code>list()</code> Immediate children — both files and directories (delimiter-based) <code>upload(File)</code> Uploads a local file using its name as the key <code>upload(File, TransferListener)</code> Upload with progress tracking","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#return-type-dispatch-for-directories","level":3,"title":"Return-type dispatch for directories","text":"<p>When a proxy method returns <code>Stream&lt;X&gt;</code> where <code>X extends S3.Dir</code>, JAWS automatically uses a delimiter-based listing and returns only directories (commonPrefixes):</p> <pre><code>public interface Repository extends S3.Dir {\n    // Catalog extends S3.Dir → only directories are returned\n    Stream&lt;Catalog&gt; catalogs();\n}\n</code></pre> <p>This is more efficient than filtering — directories that don't match are never transferred from AWS.</p>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#s3file-files-with-content","level":2,"title":"S3.File — Files with Content","text":"<p>Interfaces that model a single S3 object with readable/writable content extend <code>S3.File</code>:</p> <pre><code>public interface Config extends S3.File {\n    // All these methods are inherited:\n    // getValueAsString(), getValueAsStream()\n    // setValueAsString(), setValueAsStream(), setValueAsFile()\n    // getETag(), getSize(), getLastModified(), getObjectMetadata()\n}\n</code></pre>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#inherited-methods","level":3,"title":"Inherited methods","text":"Method Description <code>getValueAsString()</code> Read content as a <code>String</code> <code>getValueAsStream()</code> Read content as an <code>InputStream</code> <code>setValueAsString(String)</code> Write a string as content <code>setValueAsStream(InputStream)</code> Write from a stream <code>setValueAsFile(File)</code> Write from a local file <code>getETag()</code> The object's ETag (typically MD5) <code>getSize()</code> Content length in bytes <code>getLastModified()</code> Last modified timestamp <code>getObjectMetadata()</code> Full metadata including content type","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#return-type-dispatch-for-files","level":3,"title":"Return-type dispatch for files","text":"<p>When a proxy method returns <code>Stream&lt;X&gt;</code> where <code>X extends S3.File</code>, JAWS returns only files (contents, not commonPrefixes):</p> <pre><code>public interface Album extends S3.Dir {\n    // Photo extends S3.File → only files are returned\n    Stream&lt;Photo&gt; photos();\n}\n</code></pre>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#choosing-between-dir-and-file","level":2,"title":"Choosing Between Dir and File","text":"Your interface models... Extend A folder that contains other things <code>S3.Dir</code> A single object you want to read/write <code>S3.File</code> Something where you only need <code>file()</code> and <code>parent()</code> <code>S3</code>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/directories-and-files/#example-maven-repository","level":2,"title":"Example: Maven Repository","text":"<p>A complete example modeling a Maven repository structure:</p> <pre><code>public interface Repository extends S3.Dir {\n    Stream&lt;GroupDir&gt; groups();         // GroupDir extends S3.Dir\n}\n\npublic interface GroupDir extends S3.Dir {\n    Stream&lt;ArtifactDir&gt; artifacts();  // ArtifactDir extends S3.Dir\n}\n\npublic interface ArtifactDir extends S3.Dir {\n    Stream&lt;VersionDir&gt; versions();    // VersionDir extends S3.Dir\n\n    @Name(\"maven-metadata.xml\")\n    Metadata metadata();              // Metadata extends S3.File\n}\n\npublic interface VersionDir extends S3.Dir {\n    @Name(\"pom.xml\")\n    PomFile pom();\n\n    Stream&lt;Artifact&gt; artifacts();     // Artifact extends S3.File\n}\n\npublic interface Artifact extends S3.File {\n    // getValueAsStream(), getSize(), etc.\n}\n</code></pre> <p>Usage:</p> <pre><code>Repository repo = bucket.as(Repository.class);\nrepo.groups().forEach(group -&gt; {\n    group.artifacts().forEach(artifact -&gt; {\n        artifact.versions().forEach(version -&gt; {\n            System.out.println(version.file().getAbsoluteName());\n        });\n    });\n});\n</code></pre>","path":["Guide","Directories &amp; Files"],"tags":[]},{"location":"guide/filtering/","level":1,"title":"Filtering","text":"<p>JAWS provides several filtering mechanisms. Server-side filtering with <code>@Prefix</code> reduces the data transferred from AWS. Client-side filtering with <code>@Suffix</code>, <code>@Match</code>, and <code>@Filter</code> refines results once they arrive.</p> <p>Use server-side filtering whenever possible, then layer on client-side filters from simplest to most complex.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#server-side-filtering-with-prefix","level":2,"title":"Server-Side Filtering with @Prefix","text":"<p>The <code>@Prefix</code> annotation sends a prefix filter as part of the <code>ListObjects</code> request. AWS returns only keys that begin with the specified prefix:</p> <pre><code>public interface Repository extends S3.Dir {\n    @Prefix(\"org/apache\")\n    Stream&lt;S3File&gt; apacheArtifacts();\n}\n</code></pre> <p>This is the most efficient approach — non-matching keys never leave AWS and never consume bandwidth.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#partial-prefixes","level":3,"title":"Partial prefixes","text":"<p>The prefix can be any string, including a partial key segment:</p> <pre><code>public interface Versions extends S3.Dir {\n    @Prefix(\"10.\")\n    Stream&lt;S3File&gt; version10();  // 10.1.18, 10.1.19, ...\n\n    @Prefix(\"9.0\")\n    Stream&lt;S3File&gt; version9();   // 9.0.85, 9.0.86, ...\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#client-side-filtering-with-suffix","level":2,"title":"Client-Side Filtering with @Suffix","text":"<p>The <code>@Suffix</code> annotation filters by file name ending. It is the simplest client-side filter — a shorthand for the very common case of filtering by file extension:</p> <pre><code>public interface Assets extends S3.Dir {\n    @Suffix(\".css\")\n    Stream&lt;S3File&gt; stylesheets();\n\n    @Suffix(\".js\")\n    Stream&lt;S3File&gt; scripts();\n}\n</code></pre> <p>Multiple suffixes are OR'd — a file matches if its name ends with any of them:</p> <pre><code>public interface Assets extends S3.Dir {\n    @Suffix({\".jpg\", \".png\", \".gif\"})\n    Stream&lt;S3File&gt; images();\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#excluding-by-suffix","level":3,"title":"Excluding by suffix","text":"<p>Use <code>exclude = true</code> to remove entries matching a suffix. <code>@Suffix</code> is repeatable, so you can combine include and exclude annotations:</p> <pre><code>public interface Assets extends S3.Dir {\n    // Everything except JavaScript files\n    @Suffix(value = \".js\", exclude = true)\n    Stream&lt;S3File&gt; noJavaScript();\n\n    // All .jar files except sources and javadoc jars\n    @Suffix(\".jar\")\n    @Suffix(value = {\"-sources.jar\", \"-javadoc.jar\"}, exclude = true)\n    Stream&lt;S3File&gt; binaryJars();\n}\n</code></pre> <p>Include annotations are applied first, then exclude annotations carve out exceptions from the included set.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#client-side-filtering-with-match","level":2,"title":"Client-Side Filtering with @Match","text":"<p>The <code>@Match</code> annotation filters by a regular expression on the file name. The entire name must match (implied <code>^</code> and <code>$</code>):</p> <pre><code>public interface Reports extends S3.Dir {\n    // Match daily-2025-01-15.csv, daily-2025-02-01.csv, etc.\n    @Match(\"daily-\\\\d{4}-\\\\d{2}-\\\\d{2}\\\\.csv\")\n    Stream&lt;S3File&gt; dailyReports();\n\n    // Match .jpg or .png files\n    @Match(\".*\\\\.(jpg|png)\")\n    Stream&lt;S3File&gt; images();\n}\n</code></pre> <p>Warning</p> <p><code>@Match</code> uses <code>Pattern.asMatchPredicate()</code>, so the pattern must match the entire file name. <code>@Match(\"css\")</code> matches nothing — no file is named exactly \"css\". Use <code>@Match(\".*\\\\.css\")</code> instead.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#excluding-by-regex","level":3,"title":"Excluding by regex","text":"<p>Use <code>exclude = true</code> to remove entries matching a pattern. <code>@Match</code> is repeatable, so you can combine include and exclude annotations:</p> <pre><code>public interface Assets extends S3.Dir {\n    // All CSS files except reset.css\n    @Match(\".*\\\\.css\")\n    @Match(value = \"reset\\\\.css\", exclude = true)\n    Stream&lt;S3File&gt; cssExceptReset();\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#client-side-filtering-with-filter","level":2,"title":"Client-Side Filtering with @Filter","text":"<p>The <code>@Filter</code> annotation applies an arbitrary <code>Predicate&lt;S3File&gt;</code> after results are returned from AWS. The predicate class must have a no-arg constructor:</p> <pre><code>public class IsJar implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().endsWith(\".jar\");\n    }\n}\n\npublic class IsSnapshot implements Predicate&lt;S3File&gt; {\n    @Override\n    public boolean test(S3File file) {\n        return file.getName().contains(\"SNAPSHOT\");\n    }\n}\n\npublic interface VersionDir extends S3.Dir {\n    @Filter(IsJar.class)\n    Stream&lt;S3File&gt; jars();\n\n    @Filter(IsSnapshot.class)\n    Stream&lt;S3File&gt; snapshots();\n\n    @Filter(IsJar.class)\n    @Filter(IsSnapshot.class)\n    Stream&lt;S3File&gt; snapshotJars();  // both filters applied (AND)\n}\n</code></pre> <p>Tip</p> <p>If your filter is just checking a suffix, use <code>@Suffix</code> instead. If it's a name pattern, use <code>@Match</code>. Reserve <code>@Filter</code> for cases that need access to the full <code>S3File</code> (e.g., size, metadata, path components).</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#type-level-filters","level":2,"title":"Type-Level Filters","text":"<p><code>@Suffix</code>, <code>@Match</code>, and <code>@Filter</code> can all be placed on the element type interface itself. The filter then applies automatically to every listing method that returns that type:</p> <pre><code>@Suffix(\".parquet\")\npublic interface ParquetFile extends S3.File {\n    // Any Stream&lt;ParquetFile&gt; listing will only include .parquet files\n}\n\n@Match(\"\\\\d{4}-\\\\d{2}-\\\\d{2}\\\\.csv\")\npublic interface DailyReport extends S3.File {\n    // Any Stream&lt;DailyReport&gt; listing will only include date-named CSVs\n}\n\n@Filter(IsJar.class)\npublic interface JarFile extends S3.File {\n    // Any Stream&lt;JarFile&gt; listing will only include .jar files\n}\n\npublic interface DataDir extends S3.Dir {\n    Stream&lt;ParquetFile&gt; data();      // automatically filtered to .parquet\n    Stream&lt;DailyReport&gt; reports();   // automatically filtered to date CSVs\n    Stream&lt;JarFile&gt; artifacts();     // automatically filtered to .jar\n    Stream&lt;S3File&gt; everything();     // no filter applied\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#evaluation-order","level":2,"title":"Evaluation Order","text":"<p>When multiple filter annotations are present, they are applied in a defined order — simplest and cheapest first, most complex last:</p> <ol> <li>@Prefix — server-side, in the <code>ListObjects</code> request</li> <li>@Suffix includes — client-side, <code>String.endsWith()</code></li> <li>@Suffix excludes</li> <li>@Match includes — client-side, compiled regex</li> <li>@Match excludes</li> <li>@Filter — client-side, arbitrary <code>Predicate&lt;S3File&gt;</code></li> </ol> <p>All client-side filters are AND'd together. Each filter only sees entries that already passed the previous ones. This means a <code>@Filter</code> predicate can safely assume that <code>@Suffix</code> and <code>@Match</code> have already passed.</p> <p>When both interface-level and method-level annotations are present, interface-level filters run first within each category.</p> <pre><code>@Suffix(\".jar\")                   // 1st: interface-level suffix\npublic interface JarFile extends S3.File {}\n\npublic interface VersionDir extends S3.Dir {\n    @Filter(IsSnapshot.class)     // 2nd: method-level filter\n    Stream&lt;JarFile&gt; snapshotJars();\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#input-validation-on-single-arg-methods","level":2,"title":"Input Validation on Single-Arg Methods","text":"<p>Filter annotations don't just apply to listings — they also validate input on single-arg proxy methods. When a method takes a <code>String</code> parameter and returns a typed interface, JAWS checks the name against all <code>@Suffix</code>, <code>@Match</code>, <code>@Prefix</code>, and <code>@Filter</code> annotations on both the return type and the method. If the name doesn't pass, an <code>IllegalArgumentException</code> is thrown.</p> <p>This prevents writing files that would never be read back by a listing method.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#the-problem","level":3,"title":"The problem","text":"<p>Consider a <code>Users</code> directory where all entries must be <code>.json</code> files:</p> <pre><code>@Match(\".*\\\\.json\")\npublic interface UserFile extends S3.File {\n    default String getUserName() {\n        return file().getName().replaceAll(\"\\\\.json$\", \"\");\n    }\n}\n\npublic interface Users extends S3.Dir {\n    Stream&lt;UserFile&gt; users();      // only lists .json files\n    UserFile user(String name);    // accepts any name — oops!\n}\n</code></pre> <p>Without validation, <code>users.user(\"notes.txt\")</code> would happily write a file that <code>users()</code> would never return. With validation, JAWS rejects the name at the point of the <code>user(\"notes.txt\")</code> call:</p> <pre><code>users.user(\"alice.json\");    // OK\nusers.user(\"notes.txt\");     // throws IllegalArgumentException\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#how-it-works","level":3,"title":"How it works","text":"<p>When a single-arg method like <code>UserFile user(String name)</code> is invoked:</p> <ol> <li>JAWS collects annotations from the return type (<code>UserFile</code>) and the    method (<code>user</code>).</li> <li>The combined filter is evaluated against the name.</li> <li>If the name doesn't pass, an <code>IllegalArgumentException</code> is thrown with    a message like <code>\"notes.txt\" does not match the naming constraints of UserFile</code>.</li> </ol> <p>All annotation types participate: <code>@Suffix</code>, <code>@Match</code>, <code>@Filter</code>, and <code>@Prefix</code>. Type-level annotations are checked first, then method-level annotations.</p>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#type-level-vs-method-level","level":3,"title":"Type-level vs method-level","text":"<p>Annotations can live on either the return type or the method. Both are checked:</p> <pre><code>@Suffix(\".json\")\npublic interface JsonFile extends S3.File {}\n\npublic interface DataDir extends S3.Dir {\n    // Type-level @Suffix(\".json\") validates the name\n    JsonFile data(String name);\n\n    // Method-level @Match validates the name\n    @Match(\"report-\\\\d{4}\\\\.csv\")\n    S3File report(String name);\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/filtering/#combining-annotations","level":2,"title":"Combining Annotations","text":"<p>For maximum efficiency, use <code>@Prefix</code> to reduce the result set server-side, then layer on client-side filters as needed:</p> <pre><code>public interface Repository extends S3.Dir {\n    // Server-side: only keys starting with \"org/apache\"\n    // Client-side: only .jar files\n    @Prefix(\"org/apache\")\n    @Suffix(\".jar\")\n    Stream&lt;S3File&gt; apacheJars();\n\n    // Server-side: only keys starting with \"export-\"\n    // Client-side: only .parquet files matching a date pattern\n    @Prefix(\"export-\")\n    @Suffix(\".parquet\")\n    @Match(\"export-2025-.*\\\\.parquet\")\n    Stream&lt;S3File&gt; exports2025();\n\n    // All three client-side filters together\n    @Suffix(\".jar\")\n    @Match(\".*-SNAPSHOT\\\\.jar\")\n    @Filter(IsInRange.class)\n    Stream&lt;S3File&gt; recentSnapshots();\n}\n</code></pre>","path":["Guide","Filtering"],"tags":[]},{"location":"guide/typed-proxies/","level":1,"title":"Typed Proxies","text":"<p>JAWS uses Java's dynamic proxy mechanism to turn user-defined interfaces into live views of an S3 bucket. Each method call on the proxy translates into the appropriate S3 operation based on the method's return type, name, and annotations.</p>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#how-it-works","level":2,"title":"How It Works","text":"<p>When you call <code>bucket.as(MyInterface.class)</code> or <code>s3File.as(MyInterface.class)</code>, JAWS creates a <code>java.lang.reflect.Proxy</code> backed by an <code>S3Handler</code>. The handler inspects each method invocation and dispatches it based on:</p> <ol> <li>Return type — determines the kind of operation (lookup, listing, content access)</li> <li>Method name — used as the S3 key segment (unless overridden by <code>@Name</code>)</li> <li>Annotations — fine-tune key resolution, traversal, and filtering</li> </ol>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#method-signatures","level":2,"title":"Method Signatures","text":"<p>The handler recognizes the following method signatures. In the table below, <code>keyName</code> indicates the method name is used as the S3 key segment, while <code>anyName</code> indicates the method name is not significant. <code>T</code> is any user-defined interface.</p> Signature Returns <code>T keyName()</code> proxy for child <code>keyName</code> as type <code>T</code> <code>@Name(\"keyName\") T anyName()</code> proxy for child <code>keyName</code> as type <code>T</code> <code>T anyName(String keyName)</code> proxy for child <code>keyName</code> as type <code>T</code>, validated¹ <code>S3File keyName()</code> the <code>S3File</code> for child <code>keyName</code> <code>Stream&lt;T&gt; anyName()</code> all immediate children as type <code>T</code> <code>Stream&lt;S3.File&gt; anyName()</code> all immediate files <code>Stream&lt;S3.Dir&gt; anyName()</code> all immediate directories <code>List&lt;T&gt; anyName()</code> all immediate children as a <code>List</code> <code>Set&lt;T&gt; anyName()</code> all immediate children as a <code>Set</code> <code>Collection&lt;T&gt; anyName()</code> all immediate children as a <code>List</code> <code>T[] anyName()</code> all immediate children as an array <code>@Parent T anyName()</code> proxy for the parent directory <code>@Parent(n) T anyName()</code> proxy for the ancestor <code>n</code> levels up <code>@Prefix(\"p\") Stream&lt;T&gt; anyName()</code> immediate children whose key starts with <code>p</code> (server-side) <code>@Suffix(\".x\") Stream&lt;T&gt; anyName()</code> children whose name ends with <code>.x</code> <code>@Match(\"re\") Stream&lt;T&gt; anyName()</code> children whose name matches regex <code>re</code> <code>@Filter(F.class) Stream&lt;T&gt; anyName()</code> children that pass predicate <code>F</code> <code>@Recursive Stream&lt;T&gt; anyName()</code> all descendant files as type <code>T</code> <code>@Recursive Stream&lt;S3.Dir&gt; anyName()</code> all descendant directories <p>¹ When the return type or the method carries filter annotations (<code>@Suffix</code>, <code>@Match</code>, <code>@Prefix</code>, <code>@Filter</code>), the input name is validated before the proxy is created. An <code>IllegalArgumentException</code> is thrown if the name doesn't pass. See Filtering — Input Validation.</p> <p>Filter annotations on listing methods (<code>@Suffix</code>, <code>@Match</code>, <code>@Filter</code>) apply to all collection return types — <code>Stream</code>, <code>List</code>, <code>Set</code>, <code>Collection</code>, and arrays. Only <code>Stream&lt;T&gt;</code> is shown above for brevity.</p>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#examples","level":3,"title":"Examples","text":"<pre><code>public interface Module extends S3.Dir {\n    // T keyName() — child proxy\n    Source src();\n    Target target();\n\n    // @Name(\"keyName\") T anyName()\n    @Name(\"pom.xml\")\n    PomFile pomXml();\n\n    // S3File keyName()\n    S3File readme();\n}\n\n@Match(\".*\\\\.json\")\npublic interface UserFile extends S3.File {}\n\npublic interface Users extends S3.Dir {\n    // Stream&lt;T&gt; anyName() — immediate listing, filtered by @Match on UserFile\n    Stream&lt;UserFile&gt; users();\n\n    // T anyName(String keyName) — validated against @Match on UserFile\n    UserFile user(String name);\n}\n\nusers.user(\"alice.json\");   // OK — matches @Match\nusers.user(\"notes.txt\");    // throws IllegalArgumentException\n</code></pre> <p>If a method returning <code>S3File</code> declares <code>throws FileNotFoundException</code>, the proxy will throw if the file does not exist in S3.</p>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#the-as-method","level":2,"title":"The <code>as()</code> Method","text":"<p>Proxies can be created at any level:</p> <pre><code>// From a bucket — proxy for the root\nCatalog catalog = bucket.as(Catalog.class);\n\n// From any S3File — proxy for that location\nS3File dir = bucket.getFile(\"some/path\");\nMyDir view = dir.as(MyDir.class);\n</code></pre>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#default-methods","level":2,"title":"Default Methods","text":"<p>Proxy interfaces can include default methods. They are invoked normally:</p> <pre><code>public interface Config extends S3.File {\n    default Properties asProperties() {\n        Properties p = new Properties();\n        p.load(new StringReader(getValueAsString()));\n        return p;\n    }\n}\n</code></pre>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/typed-proxies/#navigation","level":2,"title":"Navigation","text":"<p>Every proxy has access to its position in the key hierarchy:</p> <pre><code>public interface S3 {\n    S3File file();      // the S3File backing this proxy\n    S3File parent();    // the parent S3File\n}\n</code></pre> <p>For navigation beyond the immediate parent, use the <code>@Parent</code> annotation.</p>","path":["Guide","Typed Proxies"],"tags":[]},{"location":"guide/uploads-and-downloads/","level":1,"title":"Uploads &amp; Downloads","text":"<p>JAWS provides upload and download support through the AWS S3 Transfer Manager, which handles multipart transfers and retries automatically.</p>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#uploading-files","level":2,"title":"Uploading Files","text":"","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#from-s3dir","level":3,"title":"From S3.Dir","text":"<p>The <code>S3.Dir</code> interface includes an <code>upload()</code> method that uses the local file's name as the S3 key:</p> <pre><code>public interface Photos extends S3.Dir {\n    // upload() is inherited from S3.Dir\n}\n\nPhotos photos = bucket.as(Photos.class);\nphotos.upload(new File(\"/path/to/photo.jpg\"));\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#with-progress-tracking","level":3,"title":"With progress tracking","text":"<p>Pass a <code>TransferListener</code> to monitor upload progress:</p> <pre><code>photos.upload(new File(\"/path/to/large-video.mp4\"), new TransferListener() {\n    @Override\n    public void transferInitiated(Context.TransferInitiated context) {\n        System.out.println(\"Upload started\");\n    }\n\n    @Override\n    public void bytesTransferred(Context.BytesTransferred context) {\n        System.out.println(\"Transferred: \" + context.progressSnapshot().transferredBytes());\n    }\n\n    @Override\n    public void transferComplete(Context.TransferComplete context) {\n        System.out.println(\"Upload complete\");\n    }\n\n    @Override\n    public void transferFailed(Context.TransferFailed context) {\n        System.err.println(\"Upload failed: \" + context.exception().getMessage());\n    }\n});\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#from-s3file","level":3,"title":"From S3File","text":"<p>You can also upload directly to a specific <code>S3File</code>:</p> <pre><code>S3File target = bucket.root().file(\"path/to/destination.txt\");\ntarget.upload(new File(\"/path/to/source.txt\"));\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#from-s3bucket","level":3,"title":"From S3Bucket","text":"<p>The bucket provides low-level upload methods:</p> <pre><code>// Upload a file\nbucket.upload(\"path/to/key.txt\", new File(\"/local/file.txt\"));\n\n// Upload from an input stream\nbucket.upload(\"path/to/key.txt\", inputStream, contentLength);\n\n// Upload a string\nbucket.putObject(\"path/to/key.txt\", \"file contents\");\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#downloading-files","level":2,"title":"Downloading Files","text":"","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#from-s3bucket_1","level":3,"title":"From S3Bucket","text":"<pre><code>FileDownload download = bucket.download(\"path/to/key.txt\",\n    new File(\"/local/destination.txt\"));\n\n// Wait for completion\ndownload.completionFuture().join();\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#writing-content","level":2,"title":"Writing Content","text":"","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#through-s3file-proxy","level":3,"title":"Through S3.File proxy","text":"<p>The <code>S3.File</code> interface provides methods for writing content directly:</p> <pre><code>public interface Config extends S3.File {}\n\nConfig config = root.config();\nconfig.setValueAsString(\"{\\\"key\\\": \\\"value\\\"}\");\nconfig.setValueAsFile(new File(\"/path/to/config.json\"));\nconfig.setValueAsStream(inputStream);\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#through-s3file","level":3,"title":"Through S3File","text":"<pre><code>S3File file = bucket.root().file(\"path/to/file.txt\");\nfile.setValueAsString(\"content\");\nfile.setValueAsFile(new File(\"/local/file.txt\"));\nfile.setValueAsStream(inputStream);\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#reading-content","level":2,"title":"Reading Content","text":"","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#through-s3file-proxy_1","level":3,"title":"Through S3.File proxy","text":"<pre><code>Config config = root.config();\nString content = config.getValueAsString();\nInputStream stream = config.getValueAsStream();\nlong size = config.getSize();\nString etag = config.getETag();\nInstant modified = config.getLastModified();\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/uploads-and-downloads/#through-s3file_1","level":3,"title":"Through S3File","text":"<pre><code>S3File file = bucket.root().file(\"config.json\");\nString content = file.getValueAsString();\nInputStream stream = file.getValueAsStream();\n</code></pre>","path":["Guide","Uploads &amp; Downloads"],"tags":[]},{"location":"guide/walking-and-listing/","level":1,"title":"Listing &amp; Recursion","text":"<p>JAWS supports two modes of traversing an S3 key hierarchy: immediate listing (the default) and recursive listing (with <code>@Recursive</code>). Understanding the difference is key to writing efficient S3 code.</p>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#immediate-listing-default","level":2,"title":"Immediate Listing (Default)","text":"<p>Without <code>@Recursive</code>, a stream-returning method performs a single-level listing. What \"single-level\" means depends on the element type:</p> <pre><code>public interface Repository extends S3.Dir {\n    Stream&lt;S3File&gt; files();        // flat, recursive — all descendant objects (S3.Dir method)\n    Stream&lt;S3File&gt; list();         // delimiter-based — immediate children only (S3.Dir method)\n    Stream&lt;GroupDir&gt; groups();     // GroupDir extends S3.Dir → immediate dirs only\n    Stream&lt;Artifact&gt; artifacts();  // Artifact extends S3.File → immediate files only\n}\n</code></pre> <p>Under the hood, this issues a single <code>ListObjects</code> request (plus pagination if the bucket has more than 1,000 matching keys).</p>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#recursive-listing","level":2,"title":"Recursive Listing","text":"<p>The <code>@Recursive</code> annotation switches to recursive traversal. The behavior depends on the element type:</p> <ul> <li><code>Stream&lt;S3File&gt;</code> — uses <code>files()</code>, a single flat request returning all descendant objects</li> <li><code>Stream&lt;X extends S3.Dir&gt;</code> — uses <code>walk()</code>, a multi-request tree traversal returning   only directories</li> </ul> <p>If the element type extends <code>S3.File</code>, omit <code>@Recursive</code> for best performance. A recursive listing already returns only files, so there's no benefit.</p> <pre><code>public interface Repository extends S3.Dir {\n    @Recursive\n    Stream&lt;S3File&gt; everything();\n}\n</code></pre>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#aws-request-cost","level":3,"title":"AWS Request Cost","text":"<p>For <code>@Recursive Stream&lt;S3File&gt;</code>, the listing uses a single flat <code>ListObjects</code> request (no delimiter) plus pagination. This is the most efficient way to get all objects.</p> <p>For <code>@Recursive Stream&lt;X extends S3.Dir&gt;</code>, each prefix visited in the walk generates at least one <code>ListObjects</code> request. If a single response is truncated (more than 1,000 keys), additional paginated requests are made automatically.</p> <p>Consider this bucket structure:</p> <pre><code>repository/\n  org/\n    apache/\n      maven/\n        maven-core/\n          3.9.6/\n            maven-core-3.9.6.jar\n            maven-core-3.9.6.pom\n      tomcat/\n        tomcat/\n          10.1.18/\n            tomcat-10.1.18.jar\n  com/\n    google/\n      guava/\n        guava/\n          33.0/\n            guava-33.0.jar\n</code></pre> <p>A <code>@Recursive Stream&lt;S3.Dir&gt;</code> from <code>repository/</code> visits every prefix, issuing 11 requests — one for each directory level.</p>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#combining-recursive-with-prefix","level":2,"title":"Combining @Recursive with @Prefix","text":"<p><code>@Prefix</code> narrows which keys the recursive listing returns. The prefix is sent server-side, so AWS never returns keys that don't match:</p> <pre><code>public interface Repository extends S3.Dir {\n    // Recursively list only keys under the org/ namespace\n    // com/ and junit/ are never fetched\n    @Recursive\n    @Prefix(\"org/\")\n    Stream&lt;S3File&gt; orgArtifacts();\n}\n</code></pre> <p>In a large repository with thousands of groupIds, <code>@Prefix(\"org/apache/\")</code> can significantly reduce the data transferred.</p>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#combining-recursive-with-delimiter","level":2,"title":"Combining @Recursive with @Delimiter","text":"<p>By default, <code>@Recursive Stream&lt;S3.Dir&gt;</code> uses <code>\"/\"</code> as the delimiter. The <code>@Delimiter</code> annotation lets you change how the hierarchy is split:</p> <pre><code>public interface DateIndex extends S3.Dir {\n    // Keys like \"2025-01-15\", \"2025-02-20\"\n    // Delimiter \"-\" splits on hyphens\n    @Recursive\n    @Delimiter(\"-\")\n    Stream&lt;S3.Dir&gt; byDate();\n}\n</code></pre>","path":["Guide","Listing &amp; Recursion"],"tags":[]},{"location":"guide/walking-and-listing/#choosing-the-right-approach","level":2,"title":"Choosing the Right Approach","text":"Goal Approach Requests Immediate children only <code>Stream&lt;X&gt;</code> (no <code>@Recursive</code>) 1 + pagination All descendant objects <code>@Recursive Stream&lt;S3File&gt;</code> 1 + pagination All descendant directories <code>@Recursive Stream&lt;X extends S3.Dir&gt;</code> 1 per prefix Subtree only <code>@Recursive @Prefix(\"sub/\") Stream&lt;S3File&gt;</code> 1 + pagination Single level, dirs only <code>Stream&lt;X&gt;</code> where X extends <code>S3.Dir</code> 1 + pagination Single level, files only <code>Stream&lt;X&gt;</code> where X extends <code>S3.File</code> 1 + pagination","path":["Guide","Listing &amp; Recursion"],"tags":[]}]}